---
name: er-favourable-condition-gate-live
description: LIVE per-gate ER (efficiency) entry gate on the paper tournament — TICK-HONEST thresholds (floor/ceiling/BAND per gate)
metadata: 
  node_type: memory
  type: project
  originSessionId: 9162c01b-c354-41a1-9fc3-162d48ae4678
---

Per-gate **efficiency-ratio (ER) entry gate** on the paper tournament — the [[favourable-condition-gating-vision]] made concrete. Each gate only OPENs when the tape condition suits its style. ER = Kaufman net-progress/total-path over the trailing 30 one-min closes (<0.08 chop · ≥0.18 trend). Lives in `deciders.py` (`ER_FLOOR`/`ER_CEIL`/`ER_BAND`/`efficiency_ratio`/`er_blocks`), applied in `tournament.step()` — suppresses NEW opens only (open slot rides its managed exit); logs `ER gate: <gate> OPEN suppressed`.

**★★ UPDATE 2026-07-30 — momentum-long ER floors RE-TUNED + BACKTESTED (commit `9767c67`, via the router trial [[router-trial-day2-findings-0730]]):** `ER_FLOOR = {"grind_long": 0.35, "abs_veto_long": 0.25}`. **grind_long 0.35 = VALIDATED** on a 10-day reprice (07-20..30): flips grind −$670 → **+$58** (Δ +$728) — REVERSES the 07-22 "grind loses in every ER band, floors are damage-control" read; the mid-ER band (0.20–0.35) was the real killer (rotation legs that look trendy but reverse), and a 0.35 floor excludes it. **abs_veto_long 0.25 = DISPROVEN by the same backtest (−$121) — its 55s absorption-veto IS the filter, so an ER floor removes its LOW-ER veto-caught winners (abs_veto is bimodal: wins <0.25 & ≥0.35, loses only mid-band 0.25–0.35).** RECOMMEND abs_veto → NO floor (or a mid-band 0.25–0.35 EXCLUSION ≈ +$460, code change). LESSON: a plain ER floor fits a NO-veto gate (grind); a veto/absorption gate (abs_veto) wants no floor. Backtest = inline entry-ER reprice off `data/gazbot7.db` trades × `capture.db` bars.

**★ UPDATE 2026-07-24 — rgv now runs RAW BASE (no ER band, no confirm). Two same-day operator changes:** (1) 12:27 UTC ER BAND removed + 35s absorption-confirm LIVE (commit `58a1d49`); (2) ~14:07 UTC **confirm REMOVED** (commit `09d87b6`) — `CONFIRM_GATES` emptied (mechanism kept, re-enableable). WHY removed: the 12:27 confirm was tuned on ONE week; a **2-week tick-honest re-test** (`scripts/rgv_confirm_layer.py`, 07-15..17 + 07-20..24) showed it's a blunt EXPOSURE CUT not a stabiliser — it DESTROYS the short side's robust tight-ext edge (2-wk net **+$670 raw → −$180 confirmed** at ext3.0/turn0.15) and only marginally trims the weak long bleed. So rgv reverts to its raw base (`config.py` GateSpec: ext_min **2.0**, turn 0.15, fast_slope+fast_turn, atr_min 13; scalp 2R/1-ATR). ⚠ raw ext-2.0 base → SHORT green (~+$800/2wk) but LONG bleeds unfiltered (−$1168/2wk) — the **direction-router** manages the long side (benches rgv_long on down-leans). `data/rgv_confirm_live_at.json` now records BOTH timestamps for a 3-phase report A/B (ER-band → confirm → raw base). **★ THE cross-week robust rgv finding (Saturday work): SHORT wants tight-ext (2.5–3.0) / no-flow / no-confirm; LONG is a router problem not a filter one — see [[gates-two-sided-per-side-tuning]] + `scripts/rgv_base_grid.py`.** ER-band tools `scripts/absorption_confirm_{sweep,grid}.py`. Still live: ER_FLOOR (grind/thrust) + ER_CEIL (capitulation/exhaustion); rgv = raw, no band, no confirm.

**★ TICK-HONEST re-derivation 2026-07-21 (operator caught the first cut).** My FIRST thresholds came from `scripts/gate_backtest.py` = BAR-CLOSE fills → optimistic, the wick-illusion the desk forbids ([[sims-on-tick-price]]). It was WRONG: e.g. grind_long "+$1,664 at floor 0.20" was **−$1,228** repriced on real ticks. Rebuilt honest: `scripts/gate_backtest_tickhonest.py` (entry on 1-min signal, exit repriced tick-by-tick with each gate's REAL exit) + a per-0.1-ER-band sweep. The per-band view mattered — a cumulative floor/ceiling scan assumes losers sit on ONE side of an ER line, which was false for 3 of 4 gates.

**LIVE config (deployed, desk restarted):** ★ momentum gates now ALSO carry an ATR floor (magnitude gate — don't ride a trend too small to run; `deciders.ATR_FLOOR`/`atr_blocks` in `tournament.step()`, 2026-07-22):
- `grind_long` — **ER FLOOR 0.20 + ATR FLOOR 20pt**. ⚠ loses in EVERY ER band tick-honest; the floors only cut the bleed (ATR sweep: −$1,228 nat → breakeven at ATR≥20). Net loser → **RELEGATION candidate**, floors are damage-control.
- `thrust_short` — **ER FLOOR 0.20 + ATR FLOOR 16pt**. The bleed is all at ATR<16; ER≥0.20+ATR≥16 = +$726/38tr (vs +$381 ER-only). Operator chose (b) to ER-gate it too (2026-07-22) — reversed the earlier "ungated" call once the ATR floor was added.
- `rgv_long` — **BAND 0.10–0.40**. Money is the mid-band (peak 0.3–0.4 +$488); bleeds only at extremes → +$864. (The old 0.25 ceiling strangled its best band.)
- `rgv_short` — **BAND 0.30–0.40** but currently DISABLED via switch file; only edge is that one sliver (+$507, 37tr, fragile). **RELEGATION candidate.**
- `capitulation_long` — **CEIL 0.10** · `exhaustion_short` — **CEIL 0.05** (from the tick+L2 footprint duck backtest; ⚠ fixed-exit-derived, re-validate).

**REVERT:** empty ER_FLOOR/ER_CEIL/ER_BAND/ATR_FLOOR in deciders.py + restart → inert. **RE-TUNE:** edit + restart. (ATR-floor sweep tool: `scripts/momentum_atr_sweep.py`.)

**★ grind_long = KEEP (operator, 2026-07-22), NOT relegating Saturday** — it's unproven (tail-only +EV) but the operator wants it for its SPECIALIST value: jump in EARLY on continuation and let the uncapped chandelier RIDE the big ones on a real trending day. The ATR≥20 floor is the complement — it channels grind to exactly those big-ATR trend days (won't fire in chop where it bleeds). Low downside (barely fires now, can't bleed much), convex upside (a monster trend day where it jumps early + rides). Keep it around to see it hit that day; revisit later, not this Saturday. **rgv_short stays the relegation candidate.**

**⚠ CAVEATS / forward-watch:** tick coverage was ~9 days (ONE summer regime) + 60-min hold cap — a LEAD, re-validate on a 2nd regime before trusting. Paper forward-validation, NOT real-money-proven. Roster relegate/promote still SATURDAY only [[tournament-changes-saturday-only]].

**Tooling (all DuckDB-vectorized, [[duckdb-pandas-for-analytics]]):** `gate_backtest_tickhonest.py` (★ the honest one — tick-repriced, per-band + cumulative), `footprint_backtest_duck.py` (footprint gates tick+L2), `live_er_sweep.py --days N --tournament-only` (live trades by band), `shadow_er_sweep.py --days N` (shadow variants, overfit-guarded), `er_threshold_scan.py`/`gate_backtest.py` (bar-close — OPTIMISTIC, don't trust for live). **LESSON: for any live decision use the TICK-honest backtest, never bar-close.**
