---
name: exhaustion-short-live-fader-success
description: "exhaustion_short LIVE is a strong roll-over-chop FADER (07-30 4/4 +$372) — its red shadow (exhaustion_rev, fixed 12pt target) massively UNDERSTATES it; judge this gate live, not by shadow."
metadata: 
  node_type: memory
  type: project
  originSessionId: fdd18525-f007-4dbb-8d0e-f435f4d4eca8
---

**07-30 finding (router trial):** exhaustion_short, armed as an operator trial during the afternoon roll-over chop (after the +900pt morning up-trend rolled over 28246→28000), went **4/4 winning lots, +$372.5** across 2 fires (fading the failed up-swings; operator claimed each via the Claim button). Day hit a new best +$1272.

**This CORRECTS the prior read.** Memory [[exhaustion-short-counter-veto]] / [[router-exhaustion-short-null]] had exhaustion_short as MARGINAL / relegation-watch — but that verdict leaned on its SHADOW (`exhaustion_rev`), which showed **−157 the same day**. The gap is the exit: the shadow uses a fixed **8pt-stop / 12pt-target** snap-back (~$24/lot cap), while the LIVE gate uses dual-lot scale-out + chandelier + the operator's manual Claim, which **ride the fade's whole body** (~$40–117/lot realised). So the shadow's tiny fixed target throws away the edge → shadow P&L is NOT this gate's real edge. Reframes the [[exhaustion-short-400-net-tuning]] sweep too: don't tune the gate on shadow P&L. See also [[shadow-sim-understates-losses]] (inverse: for footprint faders the fixed-target shadow understates WINS).

**When it works (the regime):** roll-over CHOP after a spent trend — price rolling off a climax high with FAILED bounces to fade, ER low (0.07–0.20), NOT a clean momentum leg. It's a FADER, so it wants absorption/exhaustion, not seller-momentum. Confirmed by aggressor: the down-drift it faded had buyers still aggressing (delta +2000, absorbed) — that's fade territory, not momentum-short territory. Pair with rgv_short (short-side reversion) + capitulation_long (buy the flush) = the chop-both-ways book.

**How to apply:** in a post-trend roll-over chop, arm exhaustion_short as a FADER (not a directional short). Leash: bench on up-resumption (>range-top + ER climbing → back to momentum longs) or 2-3 stops with no fade (=real down-trend, want abs_veto_short momentum instead). Judge it on LIVE P&L, never the red shadow. Still needs multi-day N before a permanent roster change. Related: [[us-open-dont-bench-trend-rider]], [[dual-slot-scaleout-live]].
