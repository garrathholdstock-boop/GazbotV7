---
name: ratchet-fast-feed-study
description: SCOPED study (2026-06-22) — profit ratchet runs its peak+give-back on 1m bar CLOSES (evaluated 5s but 1m-stale price); move to 5s/250ms to bank more of a spike. Measure-first; doc docs/RATCHET_FAST_FEED_STUDY_SCOPE.md
metadata: 
  node_type: memory
  type: project
  originSessionId: 67c42436-f6c3-496d-ac98-2caeb1ac27ce
---

**The finding (CORRECTED 2026-06-22 — earlier "1m stale" was WRONG):** the profit ratchet is EVALUATED every 5s (daytrade_stop_runner). Its price = `get_bars(1m)[-1].close` (us_futures_daytrade.py:255) — BUT that 1m bar is the **in-flight partial**, upserted every 5s by `sources/ibkr_realtime.py:685-691` (2026-06-11 fix); `storage.upsert` replaces the same-ts partial in place, so the mark is **≤5s fresh, NOT 1m stale**. The 1m→5s gap is ALREADY closed. RESIDUAL is sub-5s only: (a) `running_peak=max(prev,current)` samples the 5s-CLOSE not the within-5s HIGH (wick missed); (b) trigger evaluated every 5s; (c) fills after trigger, not at the executable bid/ask. Lever = 250ms quote; prize is SMALLER than first framed. Empirical give-back below is real but caused by 5s-eval/wick/fill mechanics + whipsaw, not staleness. (Both an audit agent AND I initially overstated this — verified via the ibkr_realtime inflight upsert.)

**Benefit (banked data, since 06-15):** 123 us_futures DAYTRADE_PROFIT_RATCHET exits keep **68% of (1m-)MFE** vs the **75%** design (`PROFIT_RATCHET_GIVEBACK=0.25`) → ~**+$318 upper-bound (+17% of ratchet P&L)**, ~+$127 realistic on the ~$30k paper desk. **UNDERSTATED** because `max_favorable_excursion_pct` is itself tracked on the same 1m feed (hybrid_stop_state) — the true 5s/250ms peak is higher. 5s ground-truth probe blocked by sparse 5s history (feed is NEW) — only ~12/237 exits covered; those showed the 1m peak missing ~$10.7/trade vs 5s. **We can't precisely size it yet because MFE has the same blind spot — hence MEASURE-FIRST.**

**Phase 1 SHADOW capture — BUILT + DEPLOYED 2026-06-22 (clock started):** `scripts/ratchet_shadow_capture.py` replays each closed futures trade against banked 5s bars driving the REAL `profit_ratchet_exit` from the faster feed; records would-be fast exit vs actual (delta) to a SEPARATE study db `data/ratchet_shadow_study.db` (table `ratchet_shadow`) — READ-ONLY on alphabot.db, no migration, no single-writer contention. Nightly systemd timer `alphabot-ratchet-shadow.timer` @ 23:17 UTC (deployed+enabled). Backfill: ~899 trades scored, ~73 with 5s coverage (GROWING — feed new), 9 fast-fired, net delta -$13 on the 9 (some fires turn a loss into a banked gain e.g. MCL short actual -$3/shadow +$11; others early whipsaw — far too few to conclude; ACCRUE). To analyse: query the study db. 250ms-quote shadow still TODO (needs the 250ms tick tape banked — not yet captured, only the 5s funnel snapshot). **Original study design:** Phase 1 = SHADOW-track the true peak+trigger on 5s bars + 250ms quote mid alongside the live 1m ratchet, log would-have-exited vs actual per trade. Phase 2 = quantify recoverable give-back MINUS the whipsaw tax (250ms is noisy). Phase 3 = graduate the TRIGGER (then peak) into the 250ms quote-ingestion path (broker/main.py:2169 ~250ms cache) with a tick-persistence filter; exit on the executable side (bid for long, ask for short cover). Start with 5s (already captured, less noisy), 250ms later.

**250ms = bid/ask quote stream, NOT bars** (operator-confirmed); bars are 5s/1m/3m/5m in bar_history. Ties to [[chop-scalp-study]] / [[edge-spectrum-pipeline]] / 5s ratchet feed — the exit-side payoff of that capture. Revisit once weeks of 5s/250ms history accrue.
