---
name: run-catcher-null-all-microstructure
description: "Robust NULL — a big-run START is unpredictable on ALL captured microstructure (flow, price, volume, L2 resting-depth). Stop hunting tape/book run-catchers."
metadata: 
  node_type: memory
  type: project
  originSessionId: 9162c01b-c354-41a1-9fc3-162d48ae4678
---

**2026-07-25 — ROBUST NULL: on the microstructure V7 captures, a big MNQ run's START is statistically indistinguishable from the chop around it.** Don't re-build "catch-the-sat-out-runs" gates off the tape or the resting book — three independent probes all died the same way (forward edge ≈ 0):

1. **AFC (aggressor-flow continuation)** — `scripts/afc_er_sweep.py`. The Movement-3 flow gate caught 15/17 big FLOW-LED runs but bled −$7,907 (50% coin-flip). corr(60s flow, 15-min fwd) = −0.012 (flow leads *mild reversion*, not continuation).
2. **AFC + ER conditioning** (operator's "killed in chop?" hypothesis) — REFUTED. Every 0.1 ER band negative; per-fire bleed is WORST in the *clean trend* bands (−$31/0%-win at ER 0.7–0.8 — the |F|≥150 cross fires LATE, buying exhaustion). No ER floor crosses positive. A negative-EV entry can't be ER-gated green.
3. **L2 book depletion** (the lab's one untested stone) — `scripts/l2_depletion_probe.py`. Premise study on the whole tape: does far-side top-3 depth imbalance PREDICT the forward move? corr(askshare, fwd) = −0.005 / −0.006 / **+0.006** at 15/5/3-min (sign flips = noise); %continued flat ~50% across all depletion bands. **The far side thins CONCURRENTLY (price eats it), not before** — not a leading tell.
4. **OFI (order-flow imbalance = net queue CHANGE, Cont et al.) at SECONDS** — `scripts/ofi_seconds_sweep.py`, 2026-07-25. depth.db is a clean ~4Hz/250ms capture (3.28M snaps, 935k 1s-buckets, 07-09→24). OFI computed correctly (**contemporaneous R² 0.35 L1 / 0.43 MLOFI** = classic effect loudly present) but **PREDICTIVELY DEAD at every horizon 5/15/30/60s**: OOS R²≈0, hit-rate 47–49% (BELOW coin-flip), 0 of 64 z-threshold cells clear the 2pt cost (all net ~−$4). Multi-level adds contemp fit, zero predictive lift. **This is the STRONG form of the book null: the L2 book is concurrent with price by its nature at every horizon our 250ms capture can reach — resting-level, far-depletion, AND queue-change all move WITH price, none lead.** Only unreachable frontier = sub-250ms event-time (cancellation flow / depletion RATE), which the capture cadence cannot see.
5. **OFI as a CONDITIONAL confirm/veto** (operator's idea: abs_veto_55s mechanism at 1–15s since data is granular) — `scripts/ofi_confirm_veto.py`, 2026-07-25. As a fakeout FILTER on thrust_short/grind_long it does NOT add edge — every RAW-beating cell is a FAKE win (drops the caught runs), same signature as the AFC veto. **BUT two constructive fall-outs, NOT nulls:** (a) **operator's granularity intuition validated** — where a veto preserved all runs, seconds-scale beat the 55s (grind −$894@15s vs −$1088@55s), short-horizon OFI dominates. (b) ⭐**REGIME EVIDENCE:** the same base gates were +EV in 07-05→17 (thrust +$726, grind +$16 @ $1.5) but −EV in the OFI window 07-09→24 — the clean "sharpen an earning gate" test was physically unavailable because depth.db doesn't reach the earning window. **Same gates flip sign across windows = the gates are regime-DEPENDENT, not dead.** Reinforces the redirect: regime-conditioning + the 13-15 UTC open-window concentration (census: 11/15 biggest runs there) are the untested-fertile directions, NOT more tape/book run-catchers. [[desk-green-by-end-august-goal]] [[shadow-board-as-regime-router]]

**★★ AFC CLOSED FROM BOTH SIDES 2026-07-25 (exit-side null now matches the entry-side null — stop testing AFC).** Operator's last idea: salvage the coin-flip AFC ENTRY with a sharp absorption EXIT (bank the green when the tape absorbs; cut reds fast) — the one angle never tried (all prior AFC work was entry-filtering). Result NULL, decisively: the PATH IS SYMMETRIC — median MFE $66 vs MAE $70.5 = ratio 0.94; the reds (44%) go ADVERSE-FIRST (MFE $29 vs MAE $98.5, only 11% touch $50 green before the stop), so there is NO green-first population an exit can bank. 162 absorption-exit configs all −EV; the BEST (−$8,952) doesn't even beat a dumb fixed +$25 scalp (−$8,796) → absorption tape-timing adds ZERO predictive value (tightening it worsens net). The ~22% bleed cut is just "exit sooner on a symmetric path," pure harm-reduction, not a signal. Runs kept 26/26 but ride to only +$202 (entry fires late = buys exhaustion). Scripts `afc_pathasym.py`/`afc_absorb_exit.py`. **A symmetric coin-flip cannot be rescued by a clever exit — AFC is dead ENTRY and EXIT. Redirect to regime-conditioning + the 13-15 UTC open-window (census: 11/15 biggest runs there).** (RAW AFC at the corrected $1.50 fee = −$11,426, not −$27.9k which was the $5-fee framing.)

**★ EXHAUSTIVELY CONFIRMED 2026-07-25** — the |F|≥150 flow signal was tested every way and is directionless NOISE: absorption-veto (`afc_veto_sweep.py`, 15–60s) and FADE polarity (`afc_fade_sweep.py`) both fail. The clincher: RAW fade −$7264/49% ≈ RAW continuation −$7797/49%, and 1503 fires × $5/RT = $7515 fees → **gross (pre-fee) edge ≈ 0 in BOTH directions** (fade +$251, cont −$282). Loss = the fee drag on a coin-flip. Every conditioning lens (ER/ATR/veto/confirm) only cuts bet-count, never flips the coin ($/entry pinned ~−$5, win ~50% everywhere). Loss-distribution: 97% of bleed = 559 identical −$45 stops firing at a uniform ~37% rate across all bands (no veto band exists).

**The bankable lesson:** the runs are real money (entered at the census start, tick-honest, they pay ~+$664 on 15/17) — the problem is 100% the ENTRY, and no tape/book feature we capture finds the start in real time. Bank it; stop hunting run-catchers on flow/price/volume/resting-depth. Updates [[analytics-capture-roadmap]] (L2 depth was captured to fill the microstructure gap — it's captured, but the resting-depth LEVEL doesn't predict runs). **Genuinely untested book angles (if ever revisited):** depletion RATE (speed of thinning, not level), and cancellation/pull flow (spoof-pulls vs resting size) — different features, not re-runs. Related: greenfield graves in the 2026-07-24 Friday report Movement 3.

**★★ 3-WEEK FULL-ARCHIVE RE-RUN 2026-07-25 (07-05→07-24, 22.6M ticks) — null upgraded from ~1wk-in-sample to 3wk-robust.** Unified loader `scripts/archive_data.py` (ticks.db⋃capture.db, shared `battery()`). Every 1-week temptation DIED on 3× data via the SAME four kill-modes, now proven not suspected: AFC −$27.9k/48% (all 4 ISO-weeks red, fade also red); **Donchian breakout +$570/85% → −$456/61%/9-losing-days** (B5 spike non-reproducing = cooldown-artifact); impulse-fade +$791 → +$414 but 7×-weaker + pure down-beta; absorption-fade −$9.9k; **trap-reclaim ER≥.35 +$876 = pure trend beta AND a FAKE win** (WITH-trend +$1396 / AGAINST −$519; counter-trend LONG 184tr −$2,516 — the 3/3 streak was luck; run-RETENTION overlay `full_retention.py`: RAW catches 10/60 VACUUM runs, the +$876 cut catches 0/60 = green is 100% thrown-away-runs, dead on TWO independent grounds). NOT-BETA (split by concurrent 30-min trend) was the blade: every strip-best survivor only won WITH-trend = grind beta we already harvest. Scripts full_afc/full_vacuum/full_opennews.py (uncommitted). **★ CENSUS 3-week: 197 runs** (vs 66/1wk, steady ~10-15/day), cluster mix STABLE (VACUUM 60 · OPEN/NEWS 59 dominant ~30% each), sat-out ceiling $39.9k but ~$36.5k structurally un-tradeable (no desk pre-07-19). **L2 book-depletion tell WEAKENS on the bigger 142-run depth.db sample: far-side top-3 share median 0.50, only 44% below 0.50 = SYMMETRIC at ignition** — the 07-21→24 depletion impression did NOT survive. `scripts/run_census_full.py`. ⚠ still one summer range-with-open-spikes regime.
