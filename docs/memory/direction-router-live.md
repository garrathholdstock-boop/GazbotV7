---
name: direction-router-live
description: "2026-07-24 LIVE (Fri trial, operator standing-approval) — auto direction-router benches the counter-trend reversion faders once a trend is proven; the fix for the direction-blind ER gates"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9162c01b-c354-41a1-9fc3-162d48ae4678
---

**LIVE from 2026-07-24 (Friday trial), operator STANDING-APPROVAL** (commit `48cb25f`). The fix for the direction-blind ER gates ([[direction-regime-gate-enablement]]): the per-gate ER condition reads tape CLEANLINESS not DIRECTION, so a clean DOWN-trend at ER 0.32 sits inside a long-fader's band and it fades the falling knife (07-23: rgv_long −$361 in one hour, the whole −$398 day was this). The router flips the **counter-trend REVERSION faders** off once a trend is PROVEN, back on in chop:
- **★ US-OPEN WINDOW OVERRIDE (2026-07-28, gazbot7 `ed0b247`, LIVE):** `OPEN_WINDOW_UTC=(13:15..14:00)` — across the US cash open (13:30 UTC = 15:30 Paris) `run()` subtracts CHOP_OFF from the bench-set, so the momentum gates stay ON even if the pre-open read chop (the 13:15 mark un-benches them ~15 min early → catch the big opening RUN). Faders stay regime-managed. Paired CHECK CRON `2f659bf0` (weekday 13:25 UTC): runs the router + verifies the 3 momentum gates are `on` + desk-ready, pings the operator (green or ⚠ if benched). Revert: drop the `in_open_window()` block in run().
- **★ MOMENTUM-CHOP VETO added 2026-07-27 (gazbot7 `56ef119`, LIVE):** `CHOP_OFF={grind_long, abs_veto_long, abs_veto_short}` — the router now benches the MOMENTUM gates in CHOP (they whipsaw in choppy tape), the symmetric completion of faders-off-in-trends. `desired_off("CHOP")` returns CHOP_OFF; MANAGED grew to 5. Validated on realised 07-20..27: momentum book −$757 in CHOP-regime@entry vs +$18 in TREND (robust, chop bled them 5/7 days; grind_long −$598 chop vs −$16 trend). Momentum stays ON in trends (self-selects by direction). Revert: drop CHOP_OFF from MANAGED + desired_off (or disable timer).
- ⚠ **fader MANAGED SET (2026-07-25 rehab, verified 07-26):** **`DOWN_OFF={capitulation_long}` · `UP_OFF={rgv_short}`** only. rgv_long RETIRED from roster (removed); exhaustion_short REMOVED from UP_OFF (backtested 07-26 = adding it back is $0: 0 up-trend fires, structural — it needs failed buy-flow, rare in up-trends → [[router-exhaustion-short-null]]). So: **TREND DOWN → off {capitulation_long}** · **TREND UP → off {rgv_short}** · **CHOP → all on**.
- **Momentum gates (grind_long, abs_veto_long, abs_veto_short) are NOT managed** — they fire WITH thrusts (a counter-thrust can be the reversal); benching thrust only ever cost a winner. The router should NOT manage all 6 — 3 momentum stay unmanaged, 2 faders managed.

**Mechanism (`src/gazbot7/direction_router.py`, `-m gazbot7.direction_router`):** STATELESS — each run replays the Paris day's 15-min marks from capture → current effective regime. **★ Trigger LOOSENED 2026-07-26 (gazbot7 `1ffb086`, operator-directed): raw = ER≥0.20 AND |net|≥30pt over 30 1-min bars** (was 0.25/40 — too strict, leaked counter-trend near-miss losers; see the study below). **2-mark HYSTERESIS** before the effective state flips. Writes `gate_switches.env` ONLY on a change (atomic, tournament re-reads live), logs + routine-Telegrams every flip, **HOLDs on a stale/closed feed or <30 bars**. Never raises. Timer `gazbot7-direction-router.timer` = every 15 min (`*:00/15`); stateless → threshold edits auto-apply next fire, no restart.

**★ WEEKLY STUDY (operator RULE 2026-07-26: "a Friday-night study each week — pull it apart, all timings + threshold triggers, make it better; is it blocking enough LOSERS?").** Tool `scripts/router_study.py` (READ-ONLY, fast-replay asserted == module, no drift): threshold sweep (ER×NET) + timing sweep (step/hold/window/fast_exit) + joint-best + a "blocking-enough-losers" LEAKAGE diagnostic (allowed losers split into counter-trend NEAR-MISS / chop / trend-following). Wired into the Friday pipeline (`friday_v7_maxdepth.workflow.js` part25:musings). First run: router was NOT blocking enough → loosened 0.25/40→0.20/30 (the moderate step; grid-best pinned all knobs to the edge = overfit, NOT deployed; STEP stays 15 until measured, must align to the 15-min timer). ⚠ one summer RANGE regime — re-validate after a real TREND week.

**Backtest = `scripts/direction_router_backtest.py`** (imports the module's logic + constants — NO DRIFT, validates exactly what runs). Tuned + validated Mon–Thu 2026-07-20..23: **baseline −$2408 → router −$1828 (+$580), NO negative day** (Mon +224/Tue +0/Wed +56/Thu +300); despite 8–16 state flips/day the delta stayed ≥0 — hysteresis held, whipsaw didn't eat it. Re-tune = edit the module constants (moves live + backtest together).

**Sticky-exit fix — built DORMANT (2026-07-24, commit `c823a44`).** `replay_marks(fast_exit=)`: default False = shipped (a trend flips only on HOLD consecutive same-state marks — can over-hold a trend through an alternating chop/reversal patch, e.g. 07-24 longs benched ~45min too long); True = leave a trend as soon as the last HOLD marks are all NON-(current trend). **Backtest Mon 07-20→07-24: FIX vs SHIPPED = $0 EVERY day** (the over-holds were all in zero-trade windows → faster recovery saved nothing realised; fix even adds a couple flips). So committed **DORMANT, NOT deployed** (live runs fast_exit=False, bit-identical — `test_shipped_default_is_unchanged` pins it). Flip it on only if a future day lands a trade in an over-held window. NB the live over-hold was partly a partial-bar-vs-replay artifact the logic fix doesn't fully address.

⚠ **TRIAL caveats:** (1) realised trades were partly HAND-gated → +$580 is an illustrative lower bound, not a clean A/B; (2) 4 days, one instrument; (3) while live it **OWNS the 2 managed gates** (capitulation_long, rgv_short) — a manual `/off` on one is re-applied by regime (to hold one off regardless, disable the timer or relegate Saturday). Composes with the midnight [[intraday-gate-disable-hourly-sweep]] reactivator (all-on at Paris midnight, router re-applies within 15 min). **REVERT = `sudo systemctl disable --now gazbot7-direction-router.timer`.** Superset context: [[saturday-promotion-shortlist]] (this is the automated bridge to the per-gate direction-guard end-state).
