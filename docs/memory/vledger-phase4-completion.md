---
name: vledger-phase4-completion
description: "V-LEDGER Phase 4 — the \"fix every week\" cure. 4c shipped, 4b ARMED LIVE 2026-07-02, 4a pending"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6a177229-b370-41db-bb71-2814d3a9582c
---

2026-07-01 (operator: "feels like we build a fix like this every week … make sure additive+complementary not patching over patches" → scope Phase-4 completion). ARCHITECTURAL VERDICT: the position-truth model is HALF-CUT-OVER. Canonical V-LEDGER Phase 3 (ledger-as-truth, LEDGER_AUTHORITATIVE=true) is SOUND + shipped, but **Phase 4 (delete the legacy patchwork) was planned 2026-06-11 and NEVER done**. The legacy fallback layer (force_resync, _zero_ghost, _resync_db_positions_from_book, reset_from_positions, `_opened_at_from_db`, IBKR reqExecutions scan) still runs in PARALLEL. Every "weekly fix" is a bug in that legacy layer — the orphan ([[position-orphan-entry-atr-cascade]]) is literally a `_opened_at_from_db` bug (a named Phase-4 deletion target).

**The soak-gate TRAP (key finding):** Phase 4 gated on clean ledger_shadow_diffs, but it's clean=0 / ~4778 divergences/day = **281 samples × ~17 stale RETIRED-instrument phantom lots re-counted every sample** (NOT live failures). The soak measures TOTAL divergence incl. dead cruft, so it never goes green → Phase 4 never runs → cruft+fragile-paths stay → new bugs → repeat. The orphan PROVED the ledger is MORE reliable than IBKR reqExecutions (ledger had the fills IBKR was missing) → cutover is a reliability WIN.

**SCOPE `docs/VLEDGER_PHASE4_COMPLETION_SCOPE.md`:** 4c reclassify soak to ACTIVE-positions-only + one-time phantom-lot purge (unblocks the gate — DO FIRST); 4b CUT OVER opened_at/entry_exec_id/entry_atr onto the ledger (persist entry_atr at fill from bar_history@exec_time; resolve opened_at/exec_id from ledger not reqExecutions) → structurally kills the orphan class + un-muzzles max-hold; 4a DELETE the already-inert gated-off DB-mutation helpers (mechanical); force_resync SPLIT (keep ops adopt-venue-truth path, delete auto-wipe). Keep PositionBook IBKR-sync + reconcile-PAUSE (Phase 3 kept them; only reconcile-as-MUTATION retired). Each step small+tested+reversible, not big-bang. SUPERSEDES POSITION_ORPHAN_FIX_SCOPE.md F1/F2 (folded in as 4b). Success = reconcile.py shrinks, reqExecutions off the truth-path, soak passes on active book, no new weekly reconcile fix for a month.

**4c CORE BUILT+MERGED 2026-07-01** (merge 9da8e934, DEPLOY-PENDING brain restart — observe-only soak, non-urgent). `compare_projection_to_live` now excludes retired-venue lots (`crypto:*` + retired full-size) via `_is_active_book()` → soak measures the ACTIVE book only. Live DB verified: clean **False→True**, 18 divergences → **0** (17 crypto phantoms excluded, note surfaces count). The gate loop is BROKEN — the soak can now pass. 2211 ledger/reconcile tests green. REMAINING 4c step (flag-and-wait, touches ledger): one-time PURGE of the 17 retired-crypto phantom executions so they stop projecting at source (surgical — `crypto:binance_testnet:*` unclosed lots). THEN 4b (cutover opened_at/exec_id/entry_atr to ledger, kills orphan) + 4a (delete inert) unblock.

**4c COMPLETE 2026-07-01** (soak reclassify 9da8e934 + phantom purge 84c50116): 17 retired-crypto phantom lots flattened at source; soak clean=True, 0 active divergences (was clean=0 forever). Gate loop BROKEN.

**4b ARMED LIVE 2026-07-02** (merge `5e3e564d` on main; `.env LEDGER_RESOLVES_OPEN_META=true`, verified `settings.*=True`, broker HEALTHY clean boot 11:06, 0 real errors). Cuts opened_at/entry_exec_id/entry_atr off the `ib.fills()`→`_opened_at_from_db`→`now()` chain onto the executions ledger (pure `resolve_open_meta_from_ledger`, robust to the con_id-keying gap = the orphan's real mechanism). entry_atr persisted at open (migration **118** `positions.entry_atr`, Wilder ATR from bar_history@exec_time). Flag OFF = byte-identical; ROLLBACK = unset flag + paired restart (legacy path untouched = Phase 4a). Armed via flat-gated paired restart. **WATCH (accrues live, verify in sweeps): on a HELD position, opened_at/exec_id resolve from ledger + new lots get non-NULL entry_atr.** A gateway zombie (empty ib.fills()) can no longer orphan a position — [[position-orphan-entry-atr-cascade]] class structurally dead.

**⚠️ LANDMINE self-inflicted + fixed same session:** committed the 4b agent's EARLY snapshot (`ff264c2c`, migration 118 but NO constant bump) + merged to main → LATEST_SCHEMA_VERSION 117 + migration 118 = Y2.1 crash-loop trap (armed for the NEXT broker restart; the 11:06 boot survived only because bootstrap read 117 BEFORE applying 118). Caught from the agent's report, fixed by merging `4faeb083` (constant→118) to main+origin; verified 118/118/118 + 15 schema tripwire tests pass. LESSON: wait for an agent's OWN final commit — never commit its work mid-run.

**4a PENDING** (delete the now-bypassed legacy: `_opened_at_from_db` 3 callers, `force_resync` mutation — split → keep `adopt_venue_truth` ops path). Do ONLY after 4b soaks clean live a stretch. Then Phase 5 crypto cutover. Also armed 07-02: hourly flat-gated desk recycle (`alphabot-desk-recycle.timer`, broker+strategy, IBKR-flat-only) = ongoing hygiene vs broker-cache staleness.
