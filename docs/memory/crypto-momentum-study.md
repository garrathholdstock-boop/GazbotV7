---
name: crypto-momentum-study
description: "SCOPED + first-pass 2026-06-22 — a momentum ENTRY on crypto (MBT/MET/MSL) IS profitable and is NOT live (unexploited edge); but the 8d data has no bullrun so let-it-run exits can't be judged; exit is regime-dependent; tool scripts/crypto_momentum_sim.py + doc CRYPTO_MOMENTUM_STUDY_SCOPE.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: 67c42436-f6c3-496d-ac98-2caeb1ac27ce
---

**Origin:** operator asked how the desk captures a sustained crypto bullrun. Found: crypto's live entry is dip/pullback + a **mis-anchored ORB** (orb_fn pins the opening range to 09:30 New York = US *equity* open — meaningless for a 24h crypto contract). The "3-bar momentum burst" is NOT live (it's the entry_gates-empty default; live = vwap_dip,orb,vwap_pullback). The trend-continuation shadow (funnel.trend_shadow_fired) fires ~never (659/548k cycles in 5d, **0 on crypto**) and has no P&L.

**Study (`scripts/crypto_momentum_sim.py`, 1m, 06-14→06-22, long-only):** momentum ENTRY (burst = real _momentum_burst; thrust = net_atr≥1.0 + green + ATR-expanding continuation) × EXIT (scalp 25%gb/5min-cut ≈ current desk; run 50%gb no-time-cut; trail2x). Metric = capture ratio (banked÷MFE).
- **burst/scalp +$306 (62% cap), thrust/scalp +$410 (63% cap)** — momentum entries are net-POSITIVE on crypto.
- run < scalp, trail2x NEGATIVE (18% cap) — let-it-run LOSES on this data.

**MOMENTUM GATE ARMED LIVE 2026-06-22** (operator: "we need them, all contracts"): `.env FUT_ENTRY_GATES=momentum,vwap_dip,orb,vwap_pullback` (momentum FIRST, first-fires-wins). NO new code — the `_decide_gates` dispatch already routed a `"momentum"` gate to the PROVEN persistence spine (net_atr≥1.5 + green_density≥60 + ATR-expanding-or-rvol≥1.3 + 2 confirm bars; `momentum_persistence_long`) that WAS the live single-mode gate before the 2026-06-17 dip-buy experiment. **It is the MULTI-GATE config (entry_gates non-empty → _decide_gates), NOT the old FUT_ENTRY_MODE=momentum single-mode** (operator explicitly checked). Verified live on the actual desk params (strong thrust → gate=momentum). Test pinned (test_live_gate_exact). Strategy restarted (flat). **WATCH the momentum gate's live record** (it adds strength-buying entries across all contracts — untested on index live); **review all exits + the momentum gate in JULY** (folded into job #9: `crypto_momentum_sim` now runs in `regime_giveback_rerun.sh`). Reversible = drop `momentum` from FUT_ENTRY_GATES + restart. Commits dddb81d7 (test) + the .env (not git).

**REGIME-GATED 2026-06-22 (commit 10dd9139):** the gate-suite backtest (`scripts/gate_suite_backtest.py`, live entry + live ratchet exit on the 1m week) showed momentum BLED on chop (−$2,391/wk, 45% win, 1,897 fires — it over-fires marginal thrusts that revert). So momentum now **arms ONLY in a SUSTAINED trend**: `_decide_gates` requires `classify_session_regime==trending` AND `vwap_slope_atr` same sign as `net_atr` (session VWAP sloping the entry's way). session_regime alone was a near no-op (−$2,172) because net_atr≥1.5 already reads momentarily-"trending"; adding the VWAP-slope filter → 1,419 fires / −$1,361 (HALVES the chop bleed, 25% fewer fires). Still −EV in chop (it's a trend tool) but armed only when there's a real trend to ride = bullrun upside without chop churn. Backtest also: ratchet=engine (+$15-17k), flat-clock=bleed (−$20k); per-trade gate quality vwap_pullback/vwap_dip +EV, momentum/ORB −EV on the chop week; **ORB is the worst (33-34% win) even post-fix** — separate thread. REVIEW all of this in July (job #9).

**Verdict (study):** (1) **a crypto momentum/thrust entry is +EV and unexploited** — the actionable lead (arm it, measure-first). (2) scalp beats let-it-run **but only because the 8d window is all chop** (biggest hourly moves +1.6–3.8%, no bullrun) — run/trail whipsaw in chop, would flip +EV in a real trend. (3) The exit is **regime-dependent AGAIN** — scalp in chop, wide in trend → ties to [[regime-conditioned-giveback-study]]. To capture a bullrun you need BOTH the momentum entry AND a regime-gated wide exit; neither alone does it. **Can't validate the bullrun half without trend data** — re-run on a trending crypto stretch / the 5s feed (fold into mid-July job #9). Committed 24797992, no behaviour change.
