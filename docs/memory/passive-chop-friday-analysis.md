---
name: passive-chop-friday-analysis
description: 2026-06-30 BUILT — scripts/passive_chop_slope_amp_analysis.py = the Friday post-close passive_chop arm-decision tool (slope×amplitude, NOT regime) with charts
metadata:
  type: reference
---

2026-06-30 (operator: "pre-build the Friday slope×amplitude analysis, detailed, with charts showing entries+exits"). BUILT+committed `scripts/passive_chop_slope_amp_analysis.py` (main ff204046). Reads `fut_shadow_sim_trades` (strategy='passive_chop', durable, accumulates) + bar_history tape. Buckets pnl by tape SLOPE (entry_vwap_slope, ATR units) × AMPLITUDE (entry_atr_abs→atr_pct) — NOT regime (§317 proved regime useless). Outputs detailed text grid+marginals + 3 chart sets to data/analysis/passive_chop_slope_amp/: heatmap (net$ per cell), scatter (win/bleed cloud), per-symbol price TAPE with every entry+exit marked (▲/▼ entry, ○ exit, green win/red loss) over aligned slope/amp panels. **Run Friday after close: `.venv/bin/python scripts/passive_chop_slope_amp_analysis.py --days 7`** (or --since/--until for exact Mon-Fri). REQUIRES matplotlib (installed in venv via `uv pip install matplotlib`; re-install if venv rebuilt — analysis-only dep, not trading path).

PRELIMINARY (Mon-eve→Tue only, ~1 day, 365 trades, net +$1570): passive_chop's best tape = **strong-DOWN slope + LOW/v.low amplitude** (strong↓ +$1377/56%; low-amp +$942); bleeds in flat-slope+v.low-amp (dead chop, -$169/17%) and strong-UP+high-amp (chasing, -$198). High-churn mean-reverter (RATCHET 100%win +$4932 vs STOP 0% -$3878) → the slope/amp filter tilts the balance. The down-slope skew may be day-specific (MGC sold off then recovered) — NEED the full week Friday before any arm decision. See [[regime-detector-impossible-passive-pivot]].

**SCHEDULED 2026-06-30 (main 8510612d):** `alphabot-passive-chop-report.timer` runs it every **Fri 22:30 UTC** (after the weekly close, robust to DST: summer close 21:00 / winter 22:00). Wrapper `scripts/passive_chop_weekly_report.sh` (runs as user alphabot) → archives report.txt + 5 charts to `data/analysis/passive_chop_slope_amp/weekly/<DATE>/` + Telegrams the headline (net/win + slope marginals + chart path). Units in /etc/systemd/system + tracked in infra/systemd/. Wrapper self-heals matplotlib + calls .venv/bin/python directly (uv re-sync would drop the undeclared dep). First fire: Fri 2026-07-03. Manage: `systemctl list-timers alphabot-passive-chop-report.timer`; manual run `systemctl start alphabot-passive-chop-report.service` (or DRY_RUN=1 bash scripts/passive_chop_weekly_report.sh to test without Telegram).

**FULL ON/OFF ANALYSIS + amplitude ceiling 2026-07-01:** ran the slope×amp analysis (1028 trades, +$3843, 48% win, live-3). FINDING — **AMPLITUDE is the dominant on/off lever, NOT slope**: by-amp v.low +$3.0/t, low +$6.5/t, mid +$0.1/t, **high −$3.6/t**. FLOOR-vs-CEILING resolved DEFINITIVELY: at atr_pct≤0.05 the CEILING earns +$3969 (n856,+$4.6/t) while the FLOOR (amp≥0.05, passive_lit's premise) LOSES −$126 → **passive_lit's amplitude FLOOR is BACKWARDS for our tape; flip it to a CEILING.** ON=atr_pct≤0.05 (calm reverts); OFF=high-amp (volatile runs over) + dead cells (v.low×flat −$177, v.low×up −$53, mid rows). Slope secondary/non-monotonic (strong↑/↓ best, flat/mild-up worst). BUILT: added the floor-vs-ceiling+ON/OFF rule to the analysis tool, a passive_chop_calm (amp≤0.05) variant to the observe-only harness (raw net improves 3/4 on the observe-4 but spread still kills them — the edge is on the live-3), and the FULL on/off deep-dive to the Friday report. Commits f9ea16c1/edef9aea + friday wiring. Caveat: ~2 days data; shadow is a floor (no LMT fill). See [[observe-only-tick-capture-and-promotion]].

**passive_lit A/B BUILT + WIRED to Friday 2026-07-01 (scope docs/PASSIVE_LIT_FLOOR_TO_CEILING_SCOPE.md):** scripts/passive_lit_ab.py reconstructs passive_lit entries under amp{off/floor/ceiling}×slope{flat_only/all_slopes} on the live-3, desk-exit, spread-aware. RESULT (7-10d): FLOOR (passive_lit premise) worst/near-worst; CEILING beats it on all 3 (MNQ floor−$651→ceil+$489, MES→+$232-545, MGC→+$1532-1570); best ceiling threshold PER-CONTRACT (MNQ 0.05, MES/MGC 0.08). BONUS: all_slopes>flat_only (the flat-only filter also backwards — excludes winning strong-slope snapbacks). So BOTH literature levers wrong for our tape. Friday report now shows the ► per-contract verdict lines (commit 140398ec). CAVEAT: in-sample + desk-exit (NOT passive_lit own mean-target/break exit) → direction trustworthy, dollars not exact; FLIP only on a MULTI-week ceiling win. Nothing armed.

**passive_chop_calm ARMED LIVE (observe-only shadow) 2026-07-01 12:18:** added decide_passive_chop_calm to STRATEGIES (commit 1b40994b) = passive_chop fired ONLY in calm tape (atr_pct<=per-contract ceiling MNQ 0.05/MES 0.08/MGC 0.08, from the A/B). A/B partner to plain passive_chop so we WATCH the amplitude-ceiling effect on LIVE activity side-by-side. Restarted the strategy daytrade daemon on a FLAT desk (waited out an open MNQ short first — never restart with a position) → logging within 1min (10 trades). OBSERVE-ONLY, no real orders, additive (doesnt touch passive_chop data or Friday). This is passive_chop+ceiling, NOT passive_lit (still not running = the bigger P4 build). ROLLBACK: remove passive_chop_calm from STRATEGIES + restart on flat. WATCH: does calm-only lift win%/net vs passive_chop over the coming days.

**passive_chop_calm on SHADOW DASHBOARD + exit-variant BUG FIX 2026-07-01:** operator "nothing new on iphone, should be a section under chop gated". Surfaced it: added passive_chop_calm to the shadow scoreboard tuple (routes_day_review.py:410) + a passive-calm label (day_review.js SL map) + bumped the JS cache-version (commit df0edaec). Scoreboard loop is DYNAMIC + layout responsive -> the row renders on BOTH desktop+phone (no separate mobile file; region--shadow auto-places). REAL BUG: passive_chop_calm was MISSING from EXITS_BY_STRATEGY -> exits_for() fell back to the BREAKOUT exit competition (chandelier/ratchet/rtarget) so it logged 0 desk trades -> scoreboard (counts exit_variant=desk) showed em-dash. FIX: mapped passive_chop_calm to desk exit like the other passives (commit 821618bb), deleted 25 miscategorized trades, restarted strategy daemon on a FLAT desk -> calm now records under desk (endpoint n=1). OPERATOR ACTION: hard-refresh iPhone Safari to clear cached HTML/JS. Fires selectively (calm tape only) = lower volume than passive_chop.
