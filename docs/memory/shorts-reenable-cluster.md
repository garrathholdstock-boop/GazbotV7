---
name: shorts-reenable-cluster
description: "futures SHORT path — plumbing ARMED but DISARMED ON THE LIVE DESK 2026-07-13 via FUT_ENTRY_LONG_ONLY=true (MNQ long-only trial; shorts run in shadow to earn a dedicated non-mirrored gate). Re-arm = FUT_ENTRY_LONG_ONLY=false + restart."
metadata: 
  node_type: memory
  type: project
  originSessionId: 43122e6c-3e41-419a-882d-b53c02900156
---

**★ 2026-07-13 — SHORTS DISARMED ON THE LIVE DESK (long-only MNQ trial).** NOT via `_LONG_SHORT_DISCIPLINES→frozenset()` (that breaks 11 armed-state/plumbing pins + is permanent) — instead a reversible trial flag `FUT_ENTRY_LONG_ONLY=true` (.env) that the entry driver ANDs into `allows_short` (`futures_entry_driver.py` run_open_sweep: `allows_short = eurex_desk.is_opted_in_live() and not settings.FUT_ENTRY_LONG_ONLY`). Direction policy STAYS armed (plumbing + re-arm intact); shorts still run in shadow (they don't use the driver's allows_short). WHY: live momentum-short is asymmetric (MNQ shorts won 61% but netted −$133 — win-small/lose-big squeeze tails), while shadow says short momentum IS +EV → the problem is squeeze-tail risk, needs a dedicated non-mirrored gate ([[desk-l2-book-consumption-scope]] sibling: `docs/SHORT_MOMENTUM_GATE_SCOPE.md`). Re-arm = `FUT_ENTRY_LONG_ONLY=false` + restart strategy. Commit d62674a6. Fixed 5 pins latent from the MES/MGC observe-only benching (submitter tests build on benched MES → monkeypatch-unbench). Everything below is the (still-valid) plumbing history.

The futures shorts re-enable cluster (forensic #1 direction-inversion fix). Built + deployed across 2026-06-16 in tested layers; the desk traded the EXACT OPPOSITE of its short signals before this (BUY bracket on a SHORT verdict).

**RE-ARM CHECKLIST (in `alphabot/shared/direction_policy.py`) is now fully satisfied:**
1. EntryIntent carries `side` (layer 2, commit d3593a5d).
2. both futures submitters branch on `is_short` — SELL parent, stop ABOVE / TP BELOW, LMT buffered DOWN (layer 3, 1d468f74); EntryIntent + BracketOrderIntent geometry validation is side-aware.
3. long-only RAISE guard removed from both submitters (layer 3).
4. reconcile/B5 direction-policy-aware (layer 5, 5c87c94c): force_resync classifies an adopted short as EXPECTED (allows_short + within net-short cap → INFO, no pause) vs UNEXPECTED (long-only OR over-cap → CRITICAL + pause); B5 naked-short framing is direction-aware (action unchanged: pause, never auto-cover off a stale snapshot).
5. tests prove a SHORT plan places a mirror-correct SELL-to-open (tests/test_shorts_submitter_geometry.py).

Plus the **06-12 root-cause prerequisite**: the SELL-open cap (`_gate_sell_quantity_long_short`) must read ledger/IBKR-truth not the blind in-memory book — DONE & armed via `OVERSELL_GUARD_LEDGER_TRUTH=true` (`_held_qty_by_symbol_guarded` = min(book,ledger)). Net-short cap = 1 contract (`RiskLimits.max_futures_short_contracts`, operator-set for first soak).

**End-to-end test (`tests/test_shorts_end_to_end_integration.py`, commit 3d5365b1)** drives a SHORT plan through the FULL wire chain (submitter→EntryIntent→broker_client payload→SubmitBracketRequest→`_bracket_request_to_intent`→BracketOrderIntent). It caught **two long-only-era locks the unit tests missed, both of which would have made every live short FAIL at the broker** (now fixed): (1) `SubmitBracketRequest.side` was `Field(pattern="^BUY$")` → 422 on every SELL; relaxed to `^(BUY|SELL)$`. (2) `BracketOrderIntent` breakeven-latch validation was long-only (trigger>entry, floor>stop); a short's trigger is BELOW entry + the BUY-cover floor LOWERS toward entry → made side-aware. Lesson: the e2e test was load-bearing — arming without it = 100% short rejection.

**STATE: shorts are NOT armed.** `_LONG_SHORT_DISCIPLINES = frozenset()`. The running broker (restarted 2026-06-16 ~13:23 UTC in the full-stack fresh restart) does NOT yet have the 2 latest broker-side commits (`_models.py` side pattern + `order_types.py` breakeven) — they're DORMANT until shorts arm, and arming requires a broker restart anyway which will pick them up. Strategy (`alphabot-strategy-daytrade`) is UP. **Forensic kill CLEARED 2026-06-16 ~13:46 UTC (operator: "clear the kill and let it trade") — desk is LIVE again, LONGS-ONLY (shorts still disarmed).** Verified the full entry chain works post-fix: M2K built a valid 1-contract long bracket and submitted; under the kill it was `[broker_kill]`-rejected, now it goes through.

**ARMED 2026-06-18 ~08:37 UTC (operator go — first live experiment).** `_LONG_SHORT_DISCIPLINES = frozenset({"eurex_daytrade","us_futures_daytrade"})` (commit 226d7a36; state-pin tests flipped in c53d816f). Broker+strategy restarted, HEALTHY, not paused, kill clear, no errors. Net-short cap **1/symbol**. Armed slightly AHEAD of a full clean Domain-A soak (the false-naked guard [[stop-coverage-false-naked-fix]] deployed ~20min prior, verified not soaked) — accepted for a paper experiment. **DISARM = revert `_LONG_SHORT_DISCIPLINES` to frozenset() + flip the 2 state-pin tests back + restart broker+strategy.** WATCH the FIRST live short: confirm funnel action=SHORT → a real SELL order (not inverted BUY), stop ABOVE entry, cap respected, no FESX-style tangle ([[estx50-fesx-short-tangle]]).

**1st experiment (MYM ~08:40 UTC): SELL-to-open WORKED (no inversion) but exposed 2 long-only management bugs — (1) stop RE-ATTACH built a SELL trail not BUY-cover → naked → premature breakeven flatten; (2) trade-tracker long-only → short round-trip NOT recorded ('nothing in trades'). Both FIXED + DEPLOYED 2026-06-18 ~09:39 UTC (main HEAD 877938bb; commits 7533cdb6 reattach, efbaeae5 tracker+writer sign, 541efd1b policy gate). The futures ratchet was already sign-aware (futures_peak_state) — no fix. RE-ARMED for a 2nd experiment, cap 1. Watch the next short: it should now HOLD (trail+ratchet like a long) and write a side=SHORT trades row with correct P&L sign.**

**2nd experiment (~09:41-10:13 UTC): shorts HELD + recorded correctly (fixes #1/#2 validated live — re-attach placed BUY-cover trails above entry; SELL-to-open, no inversion). BUT exposed a GATE bug: `peak_rip_short` fired SHORT into a steady CLIMB — its trigger `trough_bounce_atr = (price - 20-bar-low)/atr` is trivially large in any uptrend, and the trend filter `direction_is_bear` was never wired (hardcoded False → `short_requires_bear=False` to let shorts fire at all). DISARMED + FIXED #3 (DEPLOYED ~10:13 UTC): compute_features now computes `direction_is_bear = vwap_slope_atr<0 AND price<vwap`; `short_requires_bear=True` re-enabled on DEFAULT_ENTRY_PARAMS → ALL short gates fire only in a bear tape. Verified 0 shorts post-deploy. Commits 26b51ce3 (disarm) + fc0a1da4 (filter). RE-ARM for a 3rd experiment when ready — shorts should now fire only in downtrends (fade rallies), never short a climb.**

**CURRENT STATE (verified against code 2026-06-21): shorts ARMED — 3rd experiment.** `_LONG_SHORT_DISCIPLINES = {eurex_daytrade, us_futures_daytrade}`, both desks `is_opted_in_live()=True`, `short_requires_bear=True` (fade rallies only, never short a climb). 3rd re-arm = commit **f499b27d** ("all 3 fixes deployed: re-attach, tracker, bear filter"), consistent with .env 06-18 "fire more shorts — they are today's edge" (rip-fade loosened 5.0→4.0 ATR). The "STATE: shorts are NOT armed" paragraph above is STALE (pre-3rd-experiment). The baseline test cleanup (2026-06-21) updated the direction-policy/opt-in tests to assert the ARMED state.

**★ 2026-07-12 — MOMENTUM-GATE SHORTS ARE UNCONSTRAINED TWO-SIDED (go-live relevant).** Verified for the Sunday momentum go-live: `_gate_shadow_momentum` (`futures_entry.py:817`) gates its short ONLY on `allows_short` — it does NOT apply `short_requires_bear`/`direction_is_bear` (that bear-tape filter is on the REVERSION gates only: lines ~651/903/942/960). So when routing flips to `thrust_cont`, a down-thrust shorts in ANY regime (genuinely two-sided — exactly the operator's "don't turn momentum into a long-only gate" ask), NOT bear-tape-gated like the fades. And `allows_short('us_futures_daytrade')=True` already (139 live shorts in the 10d to 07-10), so **the go-live needs NO shorts-arming flip — it's already on**; the `WEEKEND_GOLIVE_BUILD.md` "shorts arming" step is a no-op/verify, not a real flip. Net-short cap 1/symbol still applies. (I briefly mis-stated "desk is LONG_ONLY" in the 07-11 nightly analysis — WRONG; shorts have been live since the 06-18 3rd re-arm.) [[golive-shadow-momentum-to-paper]]

**Caveat (RESOLVED 2026-06-21):** the pre-existing test failures (grew to ~192) from earlier-session refactors (micros-only, gate-strip, sizing, retired-markets, schema drift) are being cleaned to FULL GREEN tonight (autonomous fan-out). NOT shorts-related. See [[golive-drop-mnq-mgc]], [[continuation-entry-status]].
