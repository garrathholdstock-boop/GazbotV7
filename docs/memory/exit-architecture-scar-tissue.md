---
name: exit-architecture-scar-tissue
description: "The desk's 9-mechanism exit spine is scar tissue for bad entries; on GOOD entries a simple fixed exit beats it. Momentum +EV (keep chandelier), reversion −EV (fix/cut entries). Two-archetype sweep, 2026-07-08."
metadata: 
  node_type: memory
  type: project
  originSessionId: d7443f59-d3b4-466f-b7e6-3903cacfd274
---

2026-07-08 operator interrogation of the exit architecture ("is it good? what if only a 1-ATR stop / an ATR trailing stop / what combos do prop desks run?"). Built `scripts/exit_architecture_study.py` + `scripts/exit_archetype_sweep.py` (`sinceMon` arg), ran on the shadow desk, 250ms ticks, all 7 contracts, via the matched `replay_exit`. Operator's spec: **two PURE archetypes, nothing else — REVERSION = fixed stop + fixed R-target; MOMENTUM = fixed 1-ATR stop + tightening ATR chandelier** (start 2.5–3.5 ATR → floor 0.5, tighten ~0.75/R).

**THE CORE INSIGHT (operator's, confirmed):** the live 9-mechanism exit spine (soft-stop + native trail + ratchet + ratchet2 + profit-lock + force-kill + flat-clock + max-hold + session) is **scar tissue built layer-on-layer to stop the bleed from BAD ENTRIES.** On a −EV book the defensive stack "wins" only by losing slower (bank the scrap, 80% win × ~$3 avg). **Fix the entries and the simple fixed exit wins** — because now there are real winners to let run, and the defensive layers just CLIP them.

**PROVEN by MFE + a natural experiment:**
- MFE: momentum median 1.91R, 18.5% reach 5R; reversion median 1.16R, only 3.3% reach 5R. → momentum winners RUN (ride them), reversion winners are bounded (bank them). Opposite exits. [[shadow-momentum-chandelier]].
- **Momentum is +EV, reversion is −EV.** Momentum +EV on all 7 (MNQ +$3.4k, MGC +$2.8k this week); reversion deeply −EV. The desk trades the LOSING archetype live (fade book) and only SHADOWS the winner — first-order finding, dwarfs the exit tuning.
- Momentum: the tuned chandelier BEATS the live spine on ALL 7 (MNQ +$1,074–1,136). Keep it, tune wider-start/faster-tighten.
- Reversion natural experiment (8-day vs since-Monday): on the −EV 8-day the spine won MNQ+MES; **this week (better entries) the simple fixed target FLIPPED to winning on MNQ+MES and went POSITIVE (+$509/+$345)** — exactly "better entries → simple exit wins". M2K/MYM stayed spine-wins = their reversion entries are STILL −EV → CUT those gates, don't defend them.

**VERDICT:** reversion = fixed ~1.5-ATR stop + ~2R target, nothing else. Momentum = 1-ATR stop + chandelier, nothing else. The complex spine only helps on −EV entries and clips winners once entries are good. **The real work is the ENTRIES, not the exit.** ⚠️ CAVEATS: overlapping shadow strategies inflate absolute $ (read DELTAS + SIGNS), in-sample, one week — a Friday LEAD (RUNBOOK addendum), NOT armed. Board is now matched-loss repriced ([[shadow-sim-understates-losses]], `SHADOW_MATCH_LIVE_EXIT=true`) so losses are honest. Earlier a 360-trade study claimed "fixed target beats spine on reversion" universally — the 8-day full sweep OVERTURNED that (spine wins on −EV reversion); classic Friday-lab-law (mid-week leads ≠ gospel).

**2026-07-08 ~18:00 Paris — DEPLOYED to the SHADOW BOARD (forward OOS test), live desk UNTOUCHED.** Operator: "adjust the shadow desk exit architecture to be only these, timestamp as of now." Flag `SHADOW_PURE_ARCHETYPE_EXITS=true` (config.py + .env): the reprice (`passive_through_fill_backtest.py::_canonical_exit_fn` + `realistic_trade`) now routes reversion→`_ex_stop_target(1.5 stop, 2.0R)`, momentum→`_ex_chandelier(3.5→0.5, t0.75, 1 stop)`, PURE (clock_min=0, no force-kill, no suppress; supersedes SHADOW_MATCH_LIVE_EXIT on the board). Verified end-to-end (STOP/TARGET/SESSION_FLAT for reversion, CHANDELIER for momentum). Change STAMPED `data/shadow_exit_archetype_changed_at.json` = 2026-07-08T15:47Z / 17:47 Paris. Reprice is INCREMENTAL → forward OOS sample = trades with entry_ts ≥ marker (board is mixed pre/post; a full re-persist to make history uniform is a separate parked op). Params are the robust NON-per-contract sweep winners; Friday can tune per contract. WATCH: does the shadow desk print money forward under simple exits (validating the thesis live)? Commit 3cfac3e9.

**MID-WEEK CHANGE DOCTRINE (operator 2026-07-08, feedback):** change things up mid-week FREELY — but ALWAYS timestamp (each change = a fresh OOS segment; never backfill). If something is CLEARLY WRONG, cut it NOW, don't wait for Friday (the cut is also timestamped). Friday = the considered cut-and-adjust for the AMBIGUOUS stuff (where only the full week tells). ⚠️ DISCRIMINATOR (mine, to not yank on noise): "clearly wrong" = STRUCTURALLY broken or bleeding with NO plausible upside (config firing nonsensically, obviously-wrong-direction) — NOT a red day or two on the shadow forward test (small-sample noise; losses are honest now → needs the week, not a reflex). Broken/no-upside = act; plausibly-fine-but-down-a-bit = let it run to Friday. Pairs with [[three-pass-adversarial-friday]] (Friday=laboratory) + timestamp-not-backfill above.
