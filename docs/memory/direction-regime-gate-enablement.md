---
name: direction-regime-gate-enablement
description: 2026-07-17 operator — standing Friday investigation — per day×regime decide which GATES and which DIRECTIONS (LONG/SHORT) are allowed to run; today SHORT +$172 / LONG −$139.5 on the SAME thrust gate
metadata: 
  node_type: memory
  type: project
  originSessionId: 9f130de1-fe42-4097-b16c-315bc9433aff
---

2026-07-17 operator — NEW Friday-report investigation, SEPARATE but connected to the chandelier / gate / condition-isolation work. Per **DAY × REGIME**, decide (a) which **GATES** are allowed to run AND (b) which **DIRECTIONS** (LONG/SHORT) are allowed. Some days SHORT wins big and LONG bleeds; other days the reverse — often the *same* gate. Operator (verbatim): *"i dont know how we make that decision, but it needs to be on the cards."*

**Evidence it's real (today, live V7 MNQ, all `thrust`, one day):** LONG 8 trades net **−$139.5** (3/8 win); SHORT 8 trades net **+$172.0** (5/8 win). Identical gate, opposite outcome purely by direction — a whole day's longs donated while the shorts paid.

**The DECISION is the open question.** Honest framing (ties to [[regime-detector-impossible-passive-pivot]]): we almost certainly CANNOT predict the day's regime / winning side at the open — so this is likely a **REACTIVE direction-bias** (lean into the side the tape is currently rewarding, measured intraday) rather than a predictive day-start switch. Same "reactive-not-predictive" principle as the chandelier tuning. This is [[favourable-condition-gating-vision]] applied to (gate × **DIRECTION** × regime), and the direction axis of [[regime-chop-veto-momentum]].

**CAPTURE GAP — the enabling work** (per [[analytics-capture-roadmap]]): V7 does NOT yet stamp a REGIME label per trade (`regime_groups: {}` empty in `web.py`; no regime column in `signals`). Can't split direction×gate by regime until a regime/condition label is captured on each trade/period. The direction split ITSELF is already queryable (`trades.side`), so the LONG-vs-SHORT-by-day cut can start immediately; the regime cut waits on capture.

**★ 2026-07-17 intraday forensic (the key refinement).** Reconstructed the day trade-by-trade: the shorts-win/longs-lose gap was set by trade #7 (~09:41 UTC) and grew MONOTONICALLY for ~10h — never once narrowed. Counterfactual: veto longs once confirmed → the day swings from −$108 (actual) to +$256; even a conservative "veto after the 3rd losing long" (13:00) → +$161. BUT the surprise: the **VWAP/trend regime does NOT explain it** — price was below VWAP all morning yet FLIPPED above VWAP +254 in the afternoon, and shorts STILL won (+111) while longs STILL lost (−163) in that up-regime. **Shorts beat longs in BOTH regimes.** So a naive "price>VWAP→go long" rule does the OPPOSITE of what worked. ⇒ the actionable signal today was the **realized per-side scoreboard, NOT a trend proxy** — you can't predict which side wins, you MEASURE it by trading a few of each ("lose a few early, then confirm" — operator's exact instinct, validated). The mechanism is a **PER-SIDE CIRCUIT BREAKER**, not a trend filter: after each side has N trades, if one side net ≤ −$X and the other net ≥ +$Y, stand the loser down for the session, re-arm next day. Cost: forgoes the loser's occasional winner (today one +$87.5 long).

**★ THE MECHANISM operator articulated 2026-07-17 (how we decide — the "gauge the day" framework).** Generalize the direction circuit breaker from SIDES to GATES/DAY-TYPES = a **reactive day-type ROUTER**: turn each SPECIALIST gate on only in its regime, size to confidence, let the confirmed one rip. Operator insight: grind_fast (#22) needn't survive chop — it's a RUN-DAY specialist; the job is to KNOW the day and deploy it only then. Operator's two proposed gauges + my honest read:
- *"don't trade the morning 4-6h, just bank what tape it is"* → **you don't need to stand down — the SHADOW desk already gauges the day live, for free** (every gate scored on real ticks in real time). A blackout would skip real morning edge (today's 08:13 short +$160 etc.) to learn what shadow already shows. The gauge EXISTS; the missing piece is the router that reads it and deploys live.
- *"trade with ultra-tight stops to gauge"* → **TRAP: tight stops distort a run-rider** (grind needs room; tight stops chop the very edge you're measuring → mis-classify a grind day as nothing). If a LIVE probe is wanted, use **small SIZE + the gate's real exit**, never tight stops. But shadow already probes at zero size.
- **Day-type is NOT static** (today flipped down-trend AM → up-trend PM at ~15:00, VWAP crossed +250) → the router must be **continuous/reactive**, NOT a one-shot morning verdict (that would've mis-called the afternoon).
- **The empirical crux for Friday:** is a grind/run day readable EARLY (ATR expansion, trend persistence, run-length distribution by hour 2-3)? If YES → router acts early. If NO → fall back to the reactive shadow-scoreboard (let a few trades confirm, then size up — the circuit-breaker shape). This is the same reactive-not-predictive spine as [[regime-detector-impossible-passive-pivot]] and the answer to "how do we decide" this note opened with.

**★ FIRST-CUT "is a grind day readable early?" 2026-07-17 (n=2 full days — ANECDOTAL, but promising + it found the method).** V7 clean capture is only 3 days old (fresh at go-live); replayed grind_fast (#22) over each UTC day + measured an early (first-4h) signature. Result:
- 07-16 CHOP day: grind **−$853**, early trend-efficiency **0.03**, early |move| **49pt** (price went nowhere early).
- 07-17 TREND day: grind **+$1,131**, early efficiency **0.12**, early |move| **210pt** (directional early).
- **The early TELL was trend EFFICIENCY (directional persistence), NOT ATR** — early ATR was ~13 on BOTH days, so vol level did NOT separate them; efficiency (|net move| / path length) did (0.12 vs 0.03, 4×). Candidate router feature: early directional efficiency.
- ⚠ but today's OWN hour-by-hour shifted (efficient overnight → DEAD chop 08–12 UTC eff 0.01 → big run 14–16 eff 0.10 atr 34): the character is NOT static intraday → reinforces CONTINUOUS/reactive routing over a one-shot morning verdict (a morning gauge that saw the 08–12 chop could've stood grind down before the 14:00 run).
- **★ THE SPECIALIST PAIRING is visible even in n=2:** grind (#22 run-day) vs rg_long_fast_v (#23 chop-reversion) — CHOP day 07-16: grind −$853 / rev −$88; TREND day 07-17: grind +$1,131 / rev −$155. A router running grind on the trend day + rev/nothing on the chop day AVOIDS grind's −$853 bleed. That IS the portfolio-of-specialists + day-router thesis, with early evidence. [[friday-grind-fast-deep-dive]] [[friday-rev-fast-vol-deep-dive]]
- **Friday extends this to a REAL sample via the V5 archive** (`/home/alphabot/alphabot2/data/alphabot.db` → `bar_history`, weeks of MNQ): replay both gates over every archived day, classify trend/chop, validate the early-efficiency tell + the pairing on n≫2. Method + script pattern: scratchpad `grind_day.py`.

**★★ V5-ARCHIVE REPLAY 2026-07-17 (n=25 days — the real sample; the router thesis VALIDATED, grind's standalone case DEMOLISHED).** Replayed grind_fast + rg_long_fast_v over 25 archived MNQ days (`bar_history` 1m, ceiling pnl; scratchpad `v5_replay.py`):
- **grind alone = −$7,895** (net loser, today its best day); **rev alone = +$723** (quietly net-green — the reversion #23 is the better standalone, supporting the operator's hope). ⚠ ceiling optimistic → rev likely ~breakeven real, grind worse.
- **The EARLY-CHOP VETO is the robust signal:** early-4h efficiency **< 0.05 → grind 0/9 green, avg −$776/day.** A quiet/choppy open means grind WILL bleed — a clean stand-down rule.
- **A simple ROUTER (grind if early_eff≥0.07 else rev) = +$943.5** vs always-grind −$7,895 vs always-rev +$723 — beats BOTH, an ~$8,800 swing over always-grind. It harnesses grind's green days while dodging most disasters (07-14 early 0.007: grind −1017→rev +564; 07-10: grind −1249→rev −35).
- **BUT the positive-selection tell is NOISY:** early_eff≥0.07 still eats some grind disasters (07-13 early 0.102→grind −1192; 06-17 early 0.136→grind −1264). The <0.05 VETO is robust; "when TO run grind" needs better features (ATR expansion, run-length, session-aligned window) — the Friday tuning job.
- ⚠ full-DAY efficiency is a poor trend label over 23h (net displacement small even on run days — 07-17 full_eff 0.034 despite big runs); the EARLY window is the better lens. Session boundary = UTC-day (Globex-aligned is a refinement).
- **NET:** the router/day-type framework is REAL and is the prize (not any single gate). grind = run-day component behind the router only; rev = the chop/floor component. Friday: rebuild the router on better early features + honest repriced pnl, walk-forward.

**★★★ ROUTER DEVELOPMENT — WALK-FORWARD 2026-07-17 (the operator's #1 lab-report priority; grind is a run-day SPECIALIST by design, the router is the whole thesis — NOT a disappointment that grind bleeds chop, that's the premise).** Split 25 days train(12: 06-15→06-30)/test(13: 07-01→07-17), tuned each early-feature threshold on TRAIN, applied to TEST (scratchpad `router_dev.py`). TEST baselines: always-grind −$4,447, always-rev +$316.
- **★ WINNER — early |net directional move| (`e_move`, first-4h |close move| in pts), thr ≥153: TEST +$1,572** — beat BOTH standalones OOS (+$6k vs always-grind, +$1.3k vs always-rev). Intuition: if price has ALREADY travelled a big directional distance early → trend day → run grind; small range → chop → run rev. **This is the lead router feature.**
- early efficiency (`e_eff` thr 0.05): TEST +$135 — positive OOS but marginal vs rev.
- **early ATR (`e_atr`) FAILS OOS: −$4,006** — vol level is a TRAP (confirmed 3rd time). Do NOT route on ATR.
- US-session efficiency: collapsed to always-rev.
- **⇒ route on early DIRECTIONAL DISPLACEMENT, not volatility, not pure efficiency.** ⚠ n=25/test=13, ceiling pnl, single arbitrary split, raw-pts threshold is regime-dependent (normalize by ATR next). Read DIRECTION: e_move survives, e_atr dies. NEXT: rolling walk-forward, honest repriced pnl, ATR-normalized e_move, multi-feature, and a CONTINUOUS intraday version (today's char shifted midday).

**PROTOTYPE SHIPPED 2026-07-17** (`gazbot7/src/gazbot7/cb.py`, commit 152c370, LIVE in shadow): `cb_thrust` (governed) + `cb_thrust_dropped` (phantom stood-down side) on the board (#27/#28). `SessionDirectionBreaker` — two-sided thrust, vetoes a side when loser ≤ −loss_usd AND winner ≥ +win_usd after n_min probes; keeps trading the dropped side as a phantom + RE-ARMS if its phantom book recovers ≥ rearm_usd; resets per UTC day. Exit == chand_k35 so the ONLY diff from the control is the governor. Defaults n_min3/loss80/win80/rearm120. Offline replay over today: vetoed LONG @11:28, re-armed @16:01 then re-vetoed @16:02, dropped-long phantom −$154.5 = veto saved ~$154. ⚠ CEILING pnl in that replay is optimistic; the honest score is the repriced board going forward.

**Friday must:** (0) score `cb_thrust` vs `chand_k35` (ungoverned control) + tune the breaker thresholds `(n_min, loss_usd, win_usd, rearm_usd)` across MANY days — a rule +$ on a clear day (like today) can be flat/negative when the early sample lies; (1) split live + shadow P&L by DIRECTION × gate × day; (2) backtest the per-side circuit-breaker `(N, X, Y)` across MANY days — a rule that's +$300 today can be flat/negative on a day whose early sample lies; (3) still test regime/condition labels as a SECONDARY predictor, but the primary is realized per-side performance; (4) propose the enablement rule as an ARMABLE prescription — operator arms, never the report. Next to the condition-isolation study. See [[friday-report-full-pipeline-guarantee]] [[three-pass-adversarial-friday]] [[regime-detector-impossible-passive-pivot]].

**Why:** direction is a large, currently-unmanaged P&L axis — today a full day of thrust LONGS bled −$140 while the identical SHORT gate made +$172; running both sides blindly every day donates the losing side.
**How to apply:** carry as a standing Friday-report investigation. First build the regime/condition capture so the split is possible; then hunt the enablement rule (likely reactive, not predictive); surface it as an armable prescription. Start the direction-by-day cut now (needs no new capture).
