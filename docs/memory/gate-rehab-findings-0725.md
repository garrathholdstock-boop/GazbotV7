---
name: gate-rehab-findings-0725
description: "2026-07-25 full-roster gate rehabilitation results (grind_long, capitulation_long, rgv_long MFE) + cross-gate patterns. 3 of 4 gates rehabilitatable; every 'obvious' ER filter was fake or bug-based. Rehabbed configs → SHADOW."
metadata: 
  node_type: memory
  type: project
  originSessionId: 9162c01b-c354-41a1-9fc3-162d48ae4678
---

**2026-07-25 — applied the [[friday-gate-rehabilitation-section]] treatment (normalize→root-cause→exit→filter→robustness) to the whole roster. 3 of 4 gates are rehabilitatable; each needs a DIFFERENT primary fix (no template).** Tick-honest full archive 07-05→24, $1.50 fee. Scripts in scratchpad, uncommitted.

**grind_long — FIX-AND-KEEP (was the worst gate).** Live −$466→capped −$272 (one −$216 naked-hold = most of the exec drag; signal still −$272 = a config problem). Crux: grind DOES catch runs (39% reach ≥1.5R, runners net +$9,198) but 61% FALSE-STARTS bleed −$11,200 and swamp them. Fix (opposite of rgv): **KEEP the 2R cap** (scalp-2R BEATS chandelier/trailing — chandelier leaks, captures $41 of an $87 runner), **DROP the ER≥0.20 floor** (FAKE — runners & false-starts have identical ER 0.14), **raise ATR floor 20→24** (the real lever — grind needs volatile/trending days), **drop give-back** (minor here, NOT the killer). → **`slope0.4/fast_slope/flow0, ATR≥24, scalp-2R, no ER, no giveback` = +$1,118 (89tr, strip3 +$528)**, OOS-replicated June +$1,142 / wk28-29 +$288 / wk30 +$830. Current live (ER0.20+ATR20+chandelier+giveback) = −$528 (wrong fix). ⚠ maxDD≈net, wk29 red, 07-17=57% of net, N=89, ATR≥24 in-sample → SHADOW regime-gated first.

**capitulation_long — FIX-AND-KEEP (been fighting itself).** The "+$17 all-time green" is NOT real (live ledger has ZERO capitulation trades; came from engine/shadow noise) — faithful recon of the live config = −$510. Crux: it's on the WRONG side of its core switch — **`require_flip=True` (wait for buyers to step in at the low) is the ENTIRE edge (+$683); `require_flip=False` (fade the climax, the LIVE setting) loses −$1,173.** Give-back OFF (guts the snap-back, rgv pathology). The live **ER ceiling 0.10 is BACKWARDS + bug-based** — its provenance `footprint_backtest_duck.py` has a DuckDB integer-division bug (`ts/5000*5000` never buckets) → its "+$257" is bogus; ER FLOOR helps, not ceiling. **ATR floor ≥10** = winner-keeper + rescues the bad week. L2 bid-wall confirm additive (+$581 vs +$355) but n=18→shadow. → **`climax2.5/dom0.6/require_flip=True/ATR≥10/giveback-off/2R/ER-ungated` = +$683, all 3 weeks green, strip2 +$416, plateau.** N=63, one regime → SHADOW first.

**rgv_long — 2R is the RIGHT mechanism; the ENTRY is the constraint (MFE test).** Answered the operator's "is 2R wrong?": median MFE 2.94R and 75% of 2R-reachers run to ≥3R — so 2R DOES forfeit upside — BUT median MAE 3.24R (>MFE) and **61% stop at −1R before reaching 2R** = a falling knife. Every runner-exit (3R/4R/chandelier/trail) nets LESS than 2R; only naked-60min-no-stop beats it (+$1068) and that's a 3-trade mirage (strip-3 → −$197) needing the forbidden no-stop. Runners 100% regime-locked to down-flushes (36/38 down-trend, 0 up); fails in a real down-week (wk29 2R −$134). **→ keep 2R; rgv_long is thin/marginal (n=38) — SHADOW or RELEGATE, not live.** **★ VETO/CONFIRM RESCUE = NULL (2026-07-25, operator "not giving up"):** absorption + OFI × veto + confirm × 9 delays ALL fail — knives & clean V-bounces are NOT separable at the entry-second in tape OR book (abs-CONFIRM drops >50% of winners; abs-VETO +$358 is a mirage = wk1 + 2 days + delay-sign-flips, never reduces MAE=1R; OFI adds nothing). REAL lever: rgv_long fires 100% on down-tape and bleeds fading into ESTABLISHED down-TRENDS → a DIRECTION-ROUTER problem, not a filter one (matches the 07-24 note). **★ TREND-SEPARATION TEST (2026-07-25) = MARGINALLY SEPARABLE via net30 DEPTH — in-gate beats the router:** the losing knives concentrate in the DEEPEST down-legs (net30 < −125pt = 83% stop-first; ≥−50pt = 25%), monotone on both ATR floors, AUC 0.64–0.69. Winner-safe cut = **net30-depth FLOOR: skip LONG if trailing-30-min move < −125pt** → atr13 +$385 (keeps 13/14 winners), atr20 +$360 (6/7, all weeks green). ⚠ SHALLOW (heavy overlap, strip-3-thin, atr13 wk29 still −$57) — trims the worst tail, doesn't cleanly cleave; n=38. **★ ARCHITECTURE (operator was right): the PER-ENTRY IN-GATE net30 condition DOMINATES the day-level direction-router bench** — the router nukes the gate to −$9 (benches 60% of entries, drops 10/14 winners); the in-gate condition keeps 13/14. So build it IN `gate_reversal_grab`'s LONG side, NOT the external router. → rgv_long: "confirmed relegate" → **REVIVED LIVE 2026-07-25 18:59 UTC (commit 9f51631, operator chose LIVE not shadow)** with the in-gate net30-depth floor. Impl: `Features.net30_pt` (30-bar move in pts) + `gate_reversal_grab(net30_floor=−125)` LONG-only skip; `_RGV_LONG` = ext3.0/turn0.50/flow50/atr13/fast off/net30_floor−125, scalp-2R, give-back OFF, **base_size=1** (thin). Replaced **exhaustion_short** (→ roster 4L/2S; exhaustion QUEUED for a rehab dossier, not written off). Removed rgv_long from direction_router DOWN_OFF (self-managed via the in-gate floor — the router bench nuked it) + exhaustion from UP_OFF. ⚠ THIN/small-n/in-sample/one-regime — watch forward vs the shadow; net30 floor is the promote-or-relegate signal. [[direction-router-live]] [[rgv-tuning-both-directions]]

**exhaustion_short — FIX-AND-REVIVE (rehabbed 2026-07-25 after being dropped for rgv_long; 4-for-4).** Root cause = the EXIT (same class as grind/capitulation): the gate was BUILT with a fixed 8pt-stop/12pt-target exit (its `FootprintShadow` design) but wired to the generic ATR-2R scalp + give-back — a 24-28pt target the snap-back never reaches on a ~12pt ATR. Its own fixed 8/12 exit + give-back OFF flips it −$99 → **+$508 (n=83, 59%w), green BOTH weeks incl wk29** (the down-week that breaks rgv_long); ATR-independently corroborated (+$406 on V5 dense ticks). Malfunction: −$94→−$47 (1 bad fill). **★ ER ceiling 0.05 was BUG-BASED — the SAME DuckDB float-division bug as capitulation's, from the SAME tool `footprint_backtest_duck.py` (`ts/5000*5000` never buckets) → TWO gates crippled by one buggy tool; treat ALL its outputs as suspect.** Edge is FLOW-ABSORPTION (buying that failed to move price), NOT the ask wall (wall adds ~nothing, hurts >1.5×). Config: net400/move2/wall1.5 (wall relaxable), fixed 8/12 exit, give-back off, no ER. ⚠ n=83, one regime, recon character-faithful not trade-faithful, fixed-point exit = NEW exit type (code build) → SHADOW-first. **6TH-SLOT HEAD-TO-HEAD vs rgv_long (live): exhaustion MORE robust** (2.2× n, smoother plateau, GREEN in wk29 where rgv_long fails) but they're UNCORRELATED edges (chop flow-absorption vs down-flush reversal); rgv_long edges raw-$. Recommend (a) keep rgv_long live + SHADOW rehabbed exhaustion, relegate the loser on forward evidence. **DECISION PENDING operator** ("lets just see how it looks" — a look, nothing deployed).

**★ CROSS-GATE PATTERNS (the reusable lessons):** (1) **ER band/floor/ceiling filters are consistently FAKE or backwards** across grind/capitulation/rgv — ER reads cleanliness not the runner-vs-falsestart or direction split; one (capitulation ceiling) was literally bug-based. (2) **ATR floors are the real winner-keeping filter** (grind ≥24, capitulation ≥10, rgv_short ≥20). (3) **give-back guts snap-back scalp gates** (capitulation, rgv_long) but NOT momentum (grind). (4) Each gate's PRIMARY fix is its own: grind=keep-cap+ATR, capitulation=flip-the-switch, rgv_short=fast-off, rgv_long=entry-is-broken. **★ DEPLOYED LIVE 2026-07-25 18:26 UTC (commit 6f74a32, pushed) — operator chose LIVE for all three (not shadow):** rgv_short (own `_RGV_SHORT` dict: ext1.5/turn0.25/flow None/atr20/fast off), capitulation_long (climax2.5/dom0.6/require_flip=True + giveback off), grind_long (scalp-2R + giveback off). deciders: dropped grind ER floor + capitulation ER ceiling, ATR_FLOOR grind 24/capitulation 10, ER_HOLD moved to abs_veto_long. Roster UNCHANGED (3L/3S, same 6 tags — reconfig not roster change); exhaustion_short stays; sizes unchanged (base_size=2). Full suite green, flat/closed restart, verified OK. ⚠ all in-sample/one-regime — watch live vs the still-running shadows. **rgv_long: NOT deployed — 2R confirmed right, entry catches a knife; operator "not giving up" → running a veto/confirm rescue (absorption + OFI, both polarities) to filter the knives (agent a08e...).** This weekend = proof-of-concept for the Friday rehab section. [[friday-gate-rehabilitation-section]] [[abs-veto-live-promotion]]
