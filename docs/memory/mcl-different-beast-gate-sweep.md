---
name: mcl-different-beast-gate-sweep
description: 2026-07-01 — MCL (crude) is 2x the vol of the equity micros + event-driven; reversion gates fire it in its bleed-zone; vol-band ≤0.07 flips -$55→breakeven but no real edge — keep observe-only
metadata:
  type: project
---

2026-07-01 (operator: "MCL trades different, research + sweep the gates for a sweet spot; do web research too"). DATA: MCL median atr_pct **0.091%** = ~2x the equity micros (MNQ 0.049, MES 0.028, MGC 0.056, MYM 0.021) — its MEDIAN vol sits in the pullback "bleed zone" (≥0.09, from [[mes-entry-pullback-vol-ceiling]]). So the live reversion gates fire MCL mostly in high-vol tape where reversion fails. WEB: crude is news/event-driven (OPEC+, EIA inventories Wed 10:30 ET, geopolitics, $), moves $2-5/session; mean-reversion edge works ONLY range-bound and FADES in trends/high-vol; MCL micro spread wider than the very-liquid full-size CL.

SWEEP (`scripts/mcl_gate_sweep.py`, vol-band × rvol over 72 pullback/dip funnel entries, tighten-only): atr_pct≤0.09 = −$42; no-ceiling(live) = −$18 to −$41; **atr_pct≤0.07 = +$3 to +$5** (best cell atr_pct≤0.07/rvol≤0.3 = +$5, n=9, 66% win) vs live −$55. So gating MCL to its QUIET regime STOPS THE BLEED but yields only breakeven on thin volume (9-17 trades/wk) — and crude's wider spread (not in the 5s-close fills) would eat the +$5. VERDICT: no compelling reversion edge; **keep MCL observe-only**. If ever armed: atr_pct≤0.07 band + AVOID the EIA window (Wed 10:30 ET, scheduled vol event). Crude's character (event-driven momentum) suits breakout-around-news, not the reversion gates. Tools scripts/mcl_gate_sweep.py + gate_edge_transfer_sim.py.

**ORB SWEEP 2026-07-01 (scripts/mcl_orb_sweep.py):** simulated the dormant orb gate on MCL (09:00 ET pit-open breakout, pit-session-only, EIA-out) + live ratchet/force-kill exits. This week: ORB = **+$34 best (OR=5min + 0.25-ATR confirm, n=8, 62% win)** vs the reversion gates' −$55 — a ~$89 swing CONFIRMING crude wants momentum/breakout. Data refined the research: SHORT 5min OR + a 0.25-ATR confirmation MARGIN beats 15/30min (the margin does the whip-filtering). RATCHET_1 banked well (62% win). CAVEAT: thin (8 trades/wk), in-sample 1wk, 5s-close no spread (~$1-2/trade crude drag → real ~+$22). Promising NOT proven → the plan: route _ENTRY_GATES_BY_KEY[mcl]=(orb,) + tune ORB thresholds (5min/0.25ATR) + session/EIA filter, SHADOW-FIRST (MCL stays observe-only). NOT built yet.

**WIDER SWEEP = FLUKE CHECK 2026-07-01:** widened OR(3-30)×confirm(0-0.5) + robustness/concentration/plateau checks → the +$34/$43 is **largely a FLUKE**: only 19/42 configs positive, MEDIAN config −$3, best config +$43 is **80% ONE trade** (per-trade 34,21,13,6,1,0,-14,-20), grid SPIKY not plateau (5-7m+20m positive, 10-15m negative = noise). ONE robust lesson survives: breakout CONFIRMATION ≥0.15 ATR consistently beats a bare break (loses everywhere). So ORB is directionally less-bad than reversion (−$3 median vs −$55) but NOT a proven edge on 1wk/8 trades. REVISED PLAN: shadow-first (route mcl→orb observe-only, 5min OR + ≥0.15-ATR confirm) + accumulate MULTI-WEEK before any arm — do NOT arm on this week.
