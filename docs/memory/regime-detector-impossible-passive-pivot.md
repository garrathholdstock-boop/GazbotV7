---
name: regime-detector-impossible-passive-pivot
description: 2026-06-29 the regime/trend detector is PROVEN unpredictable at the minute scale → desk pivoted to a reactive break detector + 1s/250ms capture for passive chop; futures-only MD daemon
metadata: 
  node_type: memory
  type: project
  originSessionId: f8d854cc-966e-4451-ac84-f55a2a72b8da
---

2026-06-29 (DECISIONS §317): the operator asked to "fix the regime detector so we can re-arm the router." Deep research + measurement proved it's **IMPOSSIBLE, not a tuning bug**: trend-call accuracy = 25% = base rate = ZERO skill at every threshold/lookback/displacement-floor/forward-horizon (`scripts/regime_efficiency_calib.py`); minute-scale futures are mean-reverting (arxiv 2501.16772; ER/CHOP/ADX lag, no forward skill). **We do NOT build a regime predictor.**

**The pivot:** passive chop capture WORKS (the live `chop_capture` 34 fills netted +$48.75, killed only by the −$316 reopen run-over); the *regime CHANGE* is the enemy. So we built a **reactive BREAK DETECTOR** — calibrated on the real kills (`scripts/break_detector_calib.py`): block/exit the fade when trailing **adverse-drift ≥ 0.75 stdev + efficiency ≥ 0.20**. **Calibrated, NOT wired** (study result).

**Shipped this session (OBSERVE-ONLY, futures-only MNQ/MES/MGC):** 1s tiered `quote_snapshot` + 250ms `TickRecorder`→`data/ticks.db` (30d retention, off the trading DB; `TICK_*`/`QUOTE_FAST_*` settings); FUTURES-ONLY MD daemon (`MARKET_DATA_FUTURES_ONLY` + `UNIVERSE_DISCOVERY_ENABLED=false` killed the vestigial ~151-equity scanner — broker plane untouched); `passive_chop` shadow REPLACED `trend_donch` on the dashboard (break-detector-gated; per-contract; nightly courtroom); `scripts/passive_by_period.py` (when-do-limit-orders-pay readout).

**PATH to turn passive live (operator wants it):** shadow → data banks → courtroom + passive_by_period assess → **build the 250ms passive-LMT through-fill model** (the shadow models ENTRY only = a P&L FLOOR) → operator+courtroom arm. Watch `dip_loose` (+$93/2 vs live vwap_dip −$13/4 — candidate to loosen the LIVE dip floor). Nothing armed; live desk unchanged (reversion-only). Related [[strategy-layer-reframe]] [[chop-scalp-study]].

**UPDATE 2026-06-30 OFI ENGINE = NO-GO on our data (Phase 0 killed it):** after proving passive_chop loses realistically (−$422; leaks = trend-fades + adverse-selection fills + tight 1-ATR stop), did deep web research (Market Maker's Dilemma arxiv 2502.18625, Cont-Kukanov OFI, Avellaneda-Stoikov, mean-reverting LOB) → scoped a literature-aligned `passive_ofi` engine (`docs/PASSIVE_OFI_ENGINE_SCOPE.md`) whose centerpiece is an **OFI toxic-fill gate**. Built Phase 0 (extended `alphabot/intelligence/microstructure.py` — already had `compute_ofi`/`book_imbalance` from §287 — with `micro_price`/`micro_adjustment`/`avg_depth`/`normalized_ofi`; 12 tests). **THEN validated OFI on the real tape: corr to forward move ≈ 0 on ALL contracts at ALL windows×horizons (MNQ +0.013, swept [−0.02,+0.014]).** ROOT CAUSE: `ticks.db` is a **250ms POLL of the L1 snapshot, NOT the L1 event stream** Cont-Kukanov OFI needs (26% of polls see no change; IBKR retail L1 is itself ~250ms-throttled → the true tick L1 is NOT capturable on our infra). book_imbalance marginally better (+0.042) but too weak. **VERDICT: NO-GO — the OFI engine's best-evidenced lever has no signal on our data; build PAUSED pending a non-IBKR tick-L1 feed decision (separate scope+cost, not code).** Phase 0 did its job: validate the foundation before building. Merged e2a2ec27. The feature lib is kept (reusable). Discipline win #N: killed the engine premise in hours, before building it.

**UPDATE 2026-06-30 ALL SHADOWS re-priced realistic — EVERY shadow is realistic-NEGATIVE:** generalised the backtest to dispatch the correct ENTRY model per strategy (verified from the live order path, NOT guessed): **passive_chop/passive_chop_gated = PASSIVE limit** (`chop_capture_submit` `entry_order_type="LMT"` → through-fill or NO-TRADE); **pullback_loose/dip_loose = AGGRESSIVE marketable-LMT** (the live vwap gates, `_FUT_ENTRY_LMT_BUFFER=1.001` "crosses the spread, fills like a MKT" → pay spread, ~100% fill). In-window realistic vs ceiling: **passive_chop +$1068 ceil→ −$122 real; pullback_loose +$7→ −$19; dip_loose +$79→ −$23; passive_chop_gated −$5→ −$16.** The dip_loose I was eyeing to loosen the live dip floor is realistic-NEGATIVE → loosening would've been a mistake. Persisted per-trade to **`data/shadow_real.db`** (separate study DB, NO trading-DB migration) via `scripts/passive_through_fill_backtest.py persist`; the day-tab shadow scoreboard now shows **REALISTIC bold + ceiling dimmed** (`_shadow_day_payload` ATTACHes shadow_real.db; commit dbdf1940; dashboard restarted). DATA-RECENCY caveat: persist is a one-time batch — NEW shadow trades aren't re-priced until persist re-runs (wire into nightly). Commits 29ddaa96/dbdf1940. **VERDICT: no shadow has a realistic edge; the entire shadow stack's frictionless P&L is a mirage.**

**UPDATE 2026-06-30 v2 BUILT — passive_chop edge is a MIRAGE under realistic execution:** built the through-fill model (`alphabot/strategy/passive_fill_model.py` pure engine +10 tests; `scripts/passive_through_fill_backtest.py`). **v2 re-runs the desk's REAL exit (`exit_desk`: 1-ATR stop + ATR-ratchet + per-contract flat-clock + EOD) on 1s MID bars (the desk's REAL futures eval cadence — `.env QUOTE_FAST_INTERVAL_S=1.0` for MNQ/MES/MGC; operator corrected my 5s guess; every-250ms made the ratchet twitchy = nightmare-stuff) but FILLS at the FAR TOUCH (spread paid); 1s bar hi/lo catch intra-bar stops the laggy 1m sim missed.** RESULT (in-window passive_chop, ceiling ~+$1110): realistic = **+$5 at the most aggressive entry (rest-at-mid, touch-fill) and −$37..−$111 under genuinely passive execution**. **The +$1052 ceiling is ~100% mirage; realistic passive_chop is BREAK-EVEN-TO-NEGATIVE.** Validated per-trade (divergences both ways — real intra-bar stops + real spread, no over-penalty). Commits bbaeb2f6/f015afff/a391b3a6/36e64103 on branch `fix/schema-117-and-passive-shadow-tooling` (NOT pushed). **DO NOT ARM passive — the exciting shadow number was frictionless dream-stuff.** Two gates still open: (1) knob calibration vs REAL fills (BLOCKED — the 34 real chop_capture fills predate the 06-29 17:45 tick capture; needs new real fills in-window), (2) the same v2 realism should be applied to the OTHER shadows (pullback_loose/dip_loose) before believing ANY shadow P&L.

**UPDATE 2026-06-30 (mirage diagnosis + through-fill scope):** the passive shadow P&L is **mid-to-mid, frictionless, 100%-fill** (`shadow_sim.py:203/220` enters at mid, :185 exits at mid) → for a PASSIVE LIMIT strategy this is a **CEILING not a floor** (adverse selection only subtracts). Spread is small (MNQ $1.23/MES $1.50/MGC $2.65; even 2-spread haircut leaves +$77/13 since-restart positive) — **NOT the killer**. The killer = **adverse selection on fills** (winners revert away → never fill → no trade; losers trade through → fill offside), unquantifiable by any haircut. **Fees ARE netted** (`fee_rt` $1.62 MES/MNQ, $2.12 MGC — above the $1.50 convention, confirmed by recompute). Scoped the fix: **`docs/PASSIVE_THROUGH_FILL_MODEL_SCOPE.md`** — tick-driven passive-limit lifecycle over `ticks.db` (`quote_tick` now has `bid_sz`/`ask_sz` = queue depth; ~4 ticks/s; banking since 06-29 17:45), through-fill rule + timeout→cancel=NO-TRADE (captures adverse selection), exit at far-touch (pays spread), **calibrated against the 34 REAL live `chop_capture` fills** (06-24→06-29 = the empirical anchor). Output = mirage size (realistic vs ceiling) + fill rate. Also: schema-drift guard added to `FUTURES_DESK_PUNCHLIST.md` §1 after migration-117-bricked-the-broker incident (broker dead ~5h35m overnight, flat book so low cost; fix = bump `LATEST_SCHEMA_VERSION` to match max migration file + CI guard). DO NOT arm passive on the ceiling number.

**UPDATE 2026-06-29 evening (§318):** passive_chop is **+$315/29 (55% win) — net positive, real**. Built `passive_chop_gated` (passive only when router=CHOP) + the `router_regime` stamp (migration 117). **The afternoon "passive loses in INDEX" thesis FLIPPED:** with 9 INDEX trades, passive made **+$248 IN INDEX** — the router CHOP/INDEX flag is the weak-trend signal again, **NOT the picker** (the gated variant would've missed the run). **THE HUNT (day-tab shadow rewrite) named the real picker candidate = VWAP SLOPE:** winners faded a FLAT tape (slope +0.15), run-overs faded a FALLING tape (slope −0.46); amplitude + regime do NOT separate. Same signal the break detector measures (adverse-drift). So "when to use passive" = NOT a predictive gate (impossible); it's amplitude-floor + reactive break-exit on the slope/adverse-drift (+ maybe just run-always-tight since passive is +$315 ungated). Sharpens over weeks via the hunt + Friday §8 + the 250ms OFI. KEY DISCIPLINE LESSON: we shadowed the exciting 2-trade observation instead of arming it — the data corrected us in hours.
