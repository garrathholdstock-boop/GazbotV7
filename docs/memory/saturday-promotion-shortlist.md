---
name: saturday-promotion-shortlist
description: "Saturday roster promotion shortlist for the 6-gate paper tournament — lead candidate abs_veto_55s (top of the shadow board, a thrust fakeout-filter); the context behind each candidate for the Saturday decision"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9162c01b-c354-41a1-9fc3-162d48ae4678
---

Standing shortlist of shadow variants nominated for promotion into the live paper tournament, decided on **Saturdays only** ([[tournament-changes-saturday-only]]) off the Friday report + shadow PROMOTION FEEDER (dashboard `:8087`, auto-ranked by tick-repriced `real_pnl`). This memory holds the CONTEXT the raw feeder number doesn't.

**★ LEAD (added 2026-07-23): `abs_veto_55s`** — **#1 on the entire shadow board (+$798, n=80, 46% win)**, above every un-vetoed thrust (`thrust_loose` +$209, `thrust_aligned` +$235, `thrust_fast` −$196, `cb_thrust` −$812). It's `thrust` + a **55s delayed-entry absorption veto** (skips a thrust that gets absorbed = heavy aggressor flow failing to move price → the fakeouts that reverse into stops).
- **Mechanism (why it wins):** identical per-trade win/loss SIZES to plain thrust (~$62 win / ~$35 loss — same gate + exits); the edge is purely a higher HIT RATE. Culling `thrust_loose`→`abs_veto_55s` removes ~46 STOPs but only ~18 TARGETs (~2.5:1 favourable), win 36%→46%, expectancy +$1.4→+$10/tr. A **fakeout filter**, not a better trade.
- **Router idea KILLED (backtest `scripts/thrust_router_backtest.py`):** the plausible "veto in chop, un-vetoed in trend" split LOSES to abs_veto_55s standalone at every ER threshold ($574–668 best vs $798) — because the thesis was backwards: abs_veto_55s earns MORE in trend ($522/50%) than chop ($276/43%), and the un-vetoed thrust is the WEAK leg in trend ($62). So promote it **straight**, not regime-conditioned.
- **Duration sweep:** 55s is the participation×quality knee — 50s under-filters (+$560, more big losers), 60s over-filters (+$453, only 29tr).
- **⚠ CAVEAT before it goes LIVE:** the 45s version of this veto was PULLED from the live desk 2026-07-17 for "dropping trend-day winners" (a participation complaint, 14 shadow→4 live). The tick-repriced shadow here shows the opposite (veto strong in trend), but it's ~1wk one-regime with a modest trend sample (n=38). **Validate on a genuine strong-trend day before live.** Promote as its own thrust variant (candidate to relegate the current live `thrust_short` if it dominates head-to-head), not a blanket swap.
- **★ ROBUSTNESS BATTERY PASSED (2026-07-24, `scripts/abs_veto_robustness.py`, operator wants MONDAY promotion):** +$1448/103tr, win 50%, ~2:1 W/L, worst −$71, maxDD only −$222. (1) 5/7 days green (2 big days 07-17/07-23 carry the bulk — some concentration). (2) REGIME: profitable in BOTH chop (+$694/53tr) AND trend (+$754/50tr) — not a one-regime fluke (kills the "drops trend winners" worry). (3) WALK-FORWARD: 2nd half +$942 > 1st half +$505 — improving OOS, no decay. (4) vs un-vetoed thrust_loose: +$1040 more edge AND half the drawdown (−222 vs −522). ⚠ still ~9-day 1-lot tick-repriced. **FORWARD RUN = already live in shadow observe-only (07-24: +$218/15tr green).** PLAN to Monday: track daily shadow P&L + re-run the battery; promote if forward days stay green (esp. a red-trend day). Suppressed-open logging (tournament.py `SUPPRESSED-OPEN`, commit `d7f804d`) now makes gated-vs-ungated exact.

**★ RELEGATION / FIX side (operator flagged 2026-07-23) — the two LONG faders `rgv_long` + `capitulation_long`:** a clean down-trend day (2026-07-23, ER 0.32→ then violent chop) CHEWED both — `rgv_long` −$378.5 (5× STOP_UNFILLED fading the falling knife in ONE hour), `capitulation_long` −$188. Operator `/off`'d both intraday; `rgv_short`/`thrust_short` (aligned short) stayed green (+$310/+$102). **ROOT CAUSE:** their ER gate reads **cleanliness (ER magnitude), NOT DIRECTION** — a clean DOWN-trend at ER 0.32 sits inside `rgv_long`'s 0.10–0.40 band, so nothing blocked it fading LONG into a down-move; V7 has no direction/regime label ([[direction-regime-gate-enablement]]). **SATURDAY DECISION — pick per gate:** (A) **DIRECTION GUARD** — add a directional veto to the long-faders (e.g. no long-fade when VWAP slope / net tape bias is DOWN), the structural fix that keeps them for the chop days they earn on; OR (B) **RELEGATE** them out of the live lineup. Note both are the desk's chop-reversion earners on the RIGHT tape (rgv_long was +green on chop hours earlier same day) — a guard likely beats outright relegation, but validate. ⚠ both auto-reactivate Paris-midnight ([[intraday-gate-disable-hourly-sweep]]) — this is a ROSTER change, not the intraday switch.

**★ OPERATOR LEAN (2026-07-24): `rgv_long` = KEEP + GUARD (A), do NOT relegate.** Evidence: the direction-router went LIVE 07-24 ([[direction-router-live]]) and IS the direction-guard — it auto-benches rgv_long only in a proven down-trend. On 07-24 (mixed/chop after a whippy morning) rgv_long was the desk's BEST gate (+$92.5, a clean +$49.5 TARGET) — the gate isn't broken, it was direction-blind, and the router fixes exactly that. So the Saturday action for rgv_long is: keep it in the lineup, let the live router be its guard (no relegation, no per-gate code change needed yet). capitulation_long: same lean pending its own chop-day evidence. Revisit if the router's live trial shows it can't protect them.

Also on the board / relegation side: `rgv_short` is a live RELEGATION candidate (its only edge is the 0.30–0.40 ER sliver — but it was the top earner on the 07-23 chop/trend day, +$310); `grind_long` unproven big-trend-day specialist (kept by operator). See [[er-favourable-condition-gate-live]], [[filter-effectiveness-monitor]].
