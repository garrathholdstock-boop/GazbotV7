# SESSION 2026-08-13 — the day the books lied

Two incidents, one operator ruling, and the durability build that CLAUDE.md had been asking for since
07-30. **Read the ruling first; everything else is downstream of it.**

---

## ★★★ THE RULING — IBKR IS THE TRUTH

> **Operator: "ibkr is the truth. this is something gazbot v5 got right. never rely on our books.
> ever."**

Said after the day's second incident. It is now the desk's governing principle for position state.

| | |
|---|---|
| our books said the day made | **+$835.00** |
| IBKR's own executions (FIFO, all 24 fills) | **−$25.50** |
| divergence | **−$860.50** |

The books were not merely imprecise — they contained **$1,551 of profit from trades that never
happened**, and omitted a **−$834 naked short that did**.

---

## INCIDENT 1 — the router was dead for 10.5 hours (02:40 → 13:00Z)

`gazbot7-router-tick.timer` produced **125 consecutive no-op ticks** because the headless `claude -p`
could not authenticate:

```
ABORT: no JSON in output -> no change | raw='Failed to authenticate: OAuth session expired...'
```

The desk sat frozen on its 02:30Z switch state through all of London and into the US open.

**Every indicator stayed green.** Timer `active (enabled)`, service exiting `0/SUCCESS` every five
minutes, `sweep.py` clean, `PINNED` empty. The fail-safe writes "no change" on a parse failure —
which is exactly what a calm tape also produces. Nothing could distinguish a frozen desk from a quiet
one, and **nothing on the box was watching the router's own output.**

Fixed by `/login` (the unit runs `User=root` / `HOME=/root`, so it picks up a session re-login with
no restart). Now alarmed by **`gazbot7-router-health.timer`** — see below.

---

## INCIDENT 2 — the day-rider sold 8 lots it did not own

**The bug.** `day_rider.py`'s section-2 manage guard was `abs(net) > 1e-9 and st.get("entered")`. It
never checked `closed`, and `net` is the **shared account net**.

**The sequence.** The operator claimed the rider's 13:38 LONG at 14:26 (booked once, correctly,
+$731). At 15:53 the tournament opened `abs_veto_short`, so the account net went non-zero while
`entered` was still true for the session. The rider re-entered the manage path on a position it had
already closed, re-evaluated its **stale trail against the original entry and peak**, and every
minute booked an exit *and fired a real `MarketOrder(SELL, 2)`*:

```
15:53:21  SLD 1 @ 30117.50 + SLD 1 @ 30087.50   id=673  +$372  (phantom)
15:54:21  SLD 1 @ 30125.25 + SLD 1 @ 30095.25   id=674  +$404  (phantom)
15:55:21  SLD 2 @ 30124.75                      id=675  +$373  (phantom)
15:56:21  SLD 1 @ 30125.00 + SLD 1 @ 30095.00   id=678  +$402  (phantom)
```

**A naked 8-lot short**, while the ledger recorded +$1,551 of profit. Real cost ≈ **−$834**.

**Containment.** `day_rider=off` stopped the re-booking; `abs_veto_short=off` (execution-pathology
carve-out) stopped the position growing; `eod_flatten` took it −8 → 0; the tournament's drift halt
cleared itself once the books reconciled. The four phantom rows are flagged
`EXCLUDE:day_rider_phantom_rebook_20260813` — flagged, never deleted.

**Why nothing caught it.** Every guard was per-desk:
1. The tournament's `reconcile` runs ~every second and **did** fire (drift → halt) — but on `drift`
   the whole safety block is skipped (`max-hold` / naked-audit / re-protect / stop-breach all sit
   behind `!= "drift"`), and it has no authority over the other desk.
2. The day-rider had **no reconcile invariant at all**.
3. `multislot_core._foreign_net()` decides what is "not ours" by reading the **day-rider's own
   self-reported state file** — two books vouching for each other, no independent truth.
4. `own_flatten_verdict()` documents its own precondition — *"the caller must only reach it while its
   own state says it holds a position"* — and the caller's guard was the broken one.
5. The only cross-desk detector (`router_watch.py`'s DESK-MISMATCH) was throwing `NameError`, and the
   fix sat **un-deployed in a file while the service ran 23h-old code in memory**.

---

## INCIDENT 3 (quieter) — an orphan stop lived for two hours

The rider's 600pt venue stop (`orderId 49`, SELL 2 STP @ 29424.25) outlived the position it
protected: the claim closed at 14:26, the stop was still working at 18:00 against a flat account.

`eod_flatten` *tried* to cancel it and got **`Error 10147: not found`** — IBKR saying *"not yours to
cancel"* (it runs as clientId 6, the order belonged to clientId 4). **A permissions error that reads
exactly like an all-clear**, and was reported as one in this session before being caught.

This is the 08-06 shape: on that date a leftover stop fired with nothing behind it and opened a naked
short, booked six hours later as a gate trade nobody placed.

---

## WHAT WAS BUILT

### Safety
| component | what it owns |
|---|---|
| `src/gazbot7/deskrecon.py` | the invariant `venue == tournament + rider`, pure and testable |
| `scripts/desk_reconcile.py` + timer (**30s**) | reads IBKR **read-only** (clientId 8), confirms a breach on TWO independent reads, then stops **BOTH** desks (kill file + bench all gates + rider off) and pages. **Never re-arms** — release is a human act. Also runs the **inverse audit**: any working stop that would OPEN rather than reduce. |
| `day_rider.venue_first_ok()` | gates ENTRY / TRAIL / MANUAL_CLAIM / OPERATOR_SELL on the invariant. **NOT the 20:40 hard flat** — refusing to flatten because the books disagree turns a bookkeeping fault into an overnight position, which is strictly worse. A test pins that exemption. |
| `day_rider.cancel_own_stops()` | takes the venue stop down on all five close paths. **clientId-filtered** — it must never cancel the tournament's per-slot stops; that is the safety property, and it is tested. |

**★ On a netted shared account, "read the venue" is not enough.** IBKR nets both desks into ONE
number, so a desk **cannot** read its own position from it. Venue-first therefore means: *nobody may
act unless the whole account reconciles against every desk's claim.* An unaccounted lot means some
book is lying and you do not know which.

### Durability — the session layer no longer matters
| unit | cadence | purpose |
|---|---|---|
| `gazbot7-router-health.timer` | 5 min (`:02/5`) | ABORT streak / silent log / full-roster PIN / dead timer. **Verified against the real outage: fires on the 3rd ABORT tick, 10 min in, not 10.5h.** |
| `gazbot7-claude-job@{sweep,hour-watch,ledger-review,nightly-review,sunday-ramp}` | per timer | the five ex-session crons, now systemd. Prompts in `ops/job_prompts/*.md`. |
| `gazbot7-nightly-supervisor.timer` | 21:40Z | verify-and-repair, **not** a reboot. |
| `gazbot7-desk-reconcile.timer` | 30s | above. |

**All session `CronCreate` crons were deleted** — they would double-fire against the timers.

### Fixes
- `day_rider` manage guard: `and not st.get("closed")` (+ regression test pinning the source line)
- `day_rider` `CLOSED_ELSEWHERE` branch — venue flat at the clock while our book still holds now
  books, latches and alarms instead of silently dropping the trade (2 of 4 rider trades this week
  were missing from the ledger entirely)
- `book_trade()` guard failures now page instead of returning bare
- the trail readout rendered `ARM_PT` (150) while the rule armed at `4 × arm_atr` (~131) — it
  overstated the distance to arming by ~19pt every time it was read
- `web.py`: **six** trades queries had no `data_quality` filter, so the blotter, equity curve,
  per-gate table, two router panels and the cube all still showed the phantoms. One `_dq(c)` helper
  now, because six copies is how it drifted.

### Dashboard
- `TODAY` is now the **whole desk**, with `TRN` / `RDR` underneath — one tile, not three. It also
  fixed an inconsistency: `today` was tournament-only while `yest`/`d7`/`d30` were both desks.
- Phone header rebuilt as four wrapping rows; the document-wide horizontal scroll was **`.tabbar`**
  (7 tabs × `min-width:64px` = 448px min-content vs a 430px viewport, propagating up because nothing
  in the chain had `min-width:0`).

### Research — the Open Rider
Sims **60–63** (`odr_*_g`) added: the same 2×2 with a **drift-direction gate**. Backtested on the
rider's 68 tick-repriced trades:

| variant | n | net |
|---|---|---|
| baseline | 68 | −$476.50 |
| gate ENTRY on confirmation | 32 | **−$979.00** ← worse than nothing |
| direction-matched | 41 | +$821.50 ← **lookahead, invalid** |
| **unguarded early, direction-gated after** | 56 | **+$603.50** ← shipped |

The permission gate fails because drift confirmed on **all four days** — it suppressed no losses and
deleted the profitable early window. **Entries before confirmation are n=36 / +$502.50 (+$14.00/tr)
while the whole book is negative.** Operator: *"the fact it jumps in early is kind of its edge."*
⚠ n=68 over 4 days, one of which is a third of the sample.

---

## THE WEEK, CORRECTED

| day | tournament | day-rider | total |
|---|---|---|---|
| Mon 08-10 | −$244.50 | +$39.00 † | −$205.50 |
| Tue 08-11 | +$371.50 | +$189.00 | +$560.50 |
| Wed 08-12 | −$660.00 | +$224.50 † | −$435.50 |
| Thu 08-13 | +$104.00 | +$731.00 | +$835.00 ‡ |

† backfilled — these two rider trades were **never written to the ledger**; the week looked like 2
rider trades when there were 4. ‡ book figure; IBKR says −$25.50 for 08-13.

**The day-rider is 4-for-4 and carrying the desk** (+$1,183.50 vs the tournament's −$438).

---

## LESSONS (each one cost something today)

1. **Editing a file is not deploying it.** The `router_watch` fix sat un-deployed for 2.5h while the
   service ran 23h-old code. After editing anything a long-running service imports: restart it and
   **grep the log for the old symptom**.
2. **A permissions error can read as an all-clear.** `Error 10147` meant "not yours", not "gone".
3. **A right number beside a wrong one is worse than either alone.** The header was correct while the
   blotter and the curve carried $1,551 of phantoms — and nothing said which to believe.
4. **Silent type mismatches return zero rows, not errors.** `exit_ts` is an integer epoch; comparing
   it to an ISO string in SQLite silently matches nothing.
5. **Check whether a thing already exists before building it.** The durable event watcher had existed
   since 08-11; a duplicate was written and the live unit file briefly overwritten.
6. **A detector that halts must not stop checking.** Gating the safety block behind `!= "drift"`
   means the alarm disables the fire brigade.

---

# PART TWO — the same day, later: data, the report, and gold

## ★★★ "BACKBLAZE IS THE RECORD" — the second ruling of the day

> **Operator: "you need to look on backblaze. its all backed up there. you need to know this i dont
> need to remind you."**

Said after I surveyed the local disk, found V5's `alphabot.db` at **ZERO BYTES**, and told him there
was no V5 history. There is:

| remote | contents |
|---|---|
| `gaz:state/<ts>/` | hourly encrypted `gazbot7.db`, `shadow.db`, configs — 180 objects |
| `b2raw:gazbotv7/plain/` | Parquet tape from 07-16, never pruned — 205 objects |
| **`gaz:v5archive/alphabot/`** | **21 tables, ~440MB** |
| `b2raw:AlphabotV2/monthly/` | full V5 SQLite dump, 183MB, 2026-05-01 |

**The V5 archive: 4,934 trades / 47 days / 2026-05-23→07-15 / +$18,934**, including **574 MNQ trades
(−$3,444)** and 3,012 `us_futures_daytrade` trades (+$13,485) — the same instruments we trade now,
**more than doubling our MNQ sample**, never once examined. Also `fut_signal_funnel` (143MB),
`fut_edge_observations` (94MB), `fut_regime_routing`, `courtroom_verdicts`, `rejections`.

⚠ **Do NOT append V5 to the V7 green/red record** — 800+ symbols across crypto/ASX/TSE/HKEX; its
profit is mostly not the futures desk. Filter `discipline='us_futures_daytrade'` or `symbol='MNQ'`.

**Built:** `scripts/data_inventory.py` (instant cached read for session start, `--scan` for the slow
network truth) + `gazbot7-data-inventory.timer` 4-hourly, paging critical on staleness.

**⚠ The monitor lied twice before it worked, and both were mine:**
1. `--max-depth 3 | head -400` reported the tape 104h stale on its first run — the paths are deeper
   than 3 and `head` samples alphabetically, so the newest object was never in the sample.
2. The fix passed `--recursive` to `rclone lsl`, which is **already recursive** — unknown flag, and
   with stderr to `/dev/null` all four remotes read as MISSING.
**A check that cannot tell "nothing there" from "I failed to look" is not a check.**

## THE PIPELINE, VERIFIED END TO END

`capture.db` rolling window: **book/quotes/ticks = 5 trading days, bars = 60** (bars are 349K rows —
"effectively free", and they are what most analysis reads). Then **21:05Z** tape-mirror → Parquet,
**21:15Z** prune (only days the mirror VERIFIED), **21:20Z** → B2. Local lake 24 day-partitions.

⚠ Coverage is ragged by stream: bars 19d · depth 19d · quotes 17d · ticks 13d · book 11d.
⚠ `capture.db.book` is MNQ-only (IBKR allows 3 depth subscriptions; md holds one, depth-capture two).
**MGC L2 is in `depth.db.depth_snap`.** `book` is 41ms event-driven; `depth_snap` a 250ms sample — book
sees fleeting quotes depth.db cannot.

## THE FRIDAY REPORT, REBUILT

Full architecture in CLAUDE.md. Headline: **it had never completed unattended** — three Fridays,
three causes. Now one phase per process (memory flat ~2.3GB vs a 7.31GB peak), artifacts as
checkpoints, a **200-minute reserve for `assemble→proofread→rev2→final`**, a 20-second auth preflight,
memory raised to 5.5G+2G (the old 4G+1G was **2.2GB short of the observed peak** — it protected the box
and guaranteed the report died), and the deadline moved to **05:15Z = 07:15 Paris** after I had it
landing 30 minutes LATE.

## ★★ THE $5 FEE — why it kept coming back

> Operator: *"have you fixed the $5 fee issue? we have picked it up each week and fixed it in retrospect."*

**EIGHT scripts were still computing at $5.00/RT** — 3.3× the true $1.50. The recurrence mechanism:
- the 08-02 correction fixed the **spaced** `FEE, VPP = 5.0, 2.0` and missed the **unspaced**
  `FEE,VPP=5.0,2.0`;
- my own first sweep globbed `scripts/afc_*.py` and missed three `trap_reclaim_*`;
- and the Friday **prompt** said `$5/round-trip` while every script it called used $1.50.

Every fix was a grep, and every grep had a blind spot. **An over-charged fee never LOOKS wrong** — a
marginal edge dies, the harness reports a clean NULL, and it is recorded as "no edge there".
Any result from those eight before 08-13 is suspect; each carries a RE-RUN warning.

**`tests/test_fee_constant.py` makes it permanent**, and it is AST not regex because
`VPP, FEE_RT = 2.0, 1.50` is CORRECT while `FEE, VPP = 5.0, 2.0` is the bug and they are
indistinguishable to a pattern — my regex version flagged a dozen correct files. The name is anchored
(`^FEE(_[A-Z0-9_]+)?$`) so `FEED_STALE_S` is not mistaken for commission. 80 assignments, 0 offenders.

## GOLD — the new line of enquiry

`run_census.py --symbol` with a per-symbol multiplier (**MGC $10/pt vs MNQ $2/pt** — pricing gold at
MNQ's rate understates every figure 5× and would make it look unworthy on arithmetic alone). Gold
printed **74 runs ≥1.5×ATR last week against MNQ's 72.**

Phase **`gf_MGC` runs 7th, ahead of the MNQ clusters** so it is not what gets cut. Four constraints:
gates **invented FRESH** (no porting or re-tuning the six MNQ gates) · a **2×2
{momentum,reversion}×{long,short}** · the **full exit matrix** (tight-R scalp, wide chandelier,
dual-slot A+B, time cap) · and **assume the router benches it** — score on the HOME regime and name the
router rule, never blanket. Thin n is a SHADOW ARM, never a kill. Output is a literal
`ShadowVariant(...)` line per survivor.

## PART 0 — "are we getting better?"

Generated from the trade record each Friday and **opens the report**; also served live at
`/static/progress.html`, pinned top of the reports index. W30 was **0 green / 6, −$3,083**; W33 ran
**2 green / 4, +$688**. Router era vs before: worst day **−$1,513 → −$817**, green days **2/12 → 5/12**.
The caveat stays on the page — cumulative is still negative, 5 weeks is short, paper flatters.

## MORE LESSONS

7. **The local disk is a cache; B2 is the record.** Never conclude data does not exist without checking.
8. **A grep has a blind spot; a test does not.** Three fee sweeps missed the same eight files.
9. **Sum-of-timeouts is not a runtime.** I quoted 6.8h as if measured; real phases run 4–40 min, and
   the queries are ~0.08s — DuckDB was *slower* than sqlite at this scale. The cost is token
   generation, not I/O.
10. **A monitor that cries wolf gets switched off** — and one that cannot distinguish absence from
    failure is not a monitor at all.
